# 监控预警 · 后端实现设计 v0.2

> 本文只讲**后端如何算、如何判、如何存**。前端展现形式见 §6。
> 落地原则：**只复用 `_poll_once` 已拿到的数据，不新增任何定时采集/落盘任务。**

---

## 1. 现状盘点（先对齐事实）

查证结论，避免误解：

| 模块 | 现状 |
|------|------|
| 阈值配置读写 | ✅ 已有 `alert_config.py`（`load/save/get_threshold`） |
| σ_ref 基准 IV | ✅ 已有（`load_sigma_ref` / `get_sigma` / ack 免打扰） |
| Margin 风险度分档 | ⚠️ 已有计算（`_poll_once` 内 L1190-1213，写 `_shared_state["margin_status"]`），但**只存状态、不产生告警** |
| F 速率 | ❌ **完全没有**：无计算、无触发 |
| ATM IV 速率 | ❌ **完全没有** |
| Premium Burn | ❌ **完全没有** |
| Alert 对象 / 队列 / 端点 | ❌ **完全没有** |

**所以用户「改了阈值没反应」是必然的**——链路后半段根本没实现。本次补齐。

---

## 2. 总体数据流

```
_poll_once() 每轮（现有节奏，约 3 秒）
   │
   ├─① 算四源原始值
   │     F速率 / ATM IV速率 / Premium Burn / Margin风险度
   │
   ├─② _evaluate_alerts()  ← 与阈值比较 + 冷却 + 当日计数
   │
   ├─③ 写 _shared_state["alerts"]（Ring buffer ≤100 条）
   │     + _shared_state["active_flags"]（当前仍触发的行 → 前端变色用）
   │
   └─④ /api/dashboard 带出 alerts / active_flags
```

**关键分工**：
- **后端**：算值、判级、管冷却、管计数、维护"当前仍触发集合"
- **前端**：弹窗（按后端给的 `should_popup` 决定）、表格变色（按 `active_flags`）、历史列表

> 冷却和计数**必须放后端**。放前端会被刷新/多标签页绕过。

---

## 3. 四个触发源的计算口径

### 3.1 F 速率（F Rate）

**用途**：捕捉标的在 5min 观测窗口内的异常波动幅度（涨/跌都对卖方不利，取极差无方向）。

**公式（v0.2 定案，2026-09-21）**：
```
F_rate = ln(5min窗口内标的最高 / 最低) / (σ_ref × √(5min/年))

        5min         5
√(5min/年) = √( ────────────────────── ) = √( ───────────────────── )
               250交易日 × 60min × 日交易总时长h      250 × 60 × H
```
含义：窗口极差 = **多少个「5min 正常波动单位」**。跨品种、跨时段自动可比。

**采样与窗口**：
- 采样仍是**秒级**（`_poll_once` 每轮约 1s）；5min 只是**观测窗口**，不是采样间隔。
- 窗口规则与 IV 速率一致：`deque[(ts, price)]` 滚动 5min；相邻样本间隔 **>4h → 断档清空**（跨日/跨休市）。
- 窗口内样本 < 2 或极差为 0 → 不出值。

**输入**：
| 变量 | 来源 | 缺失处理 |
|------|------|----------|
| 标的价序列 | `underlying_prices[full_und]` | 无 → 该品种跳过 |
| `σ_ref` | `get_sigma(品种)`，缺省 0.25 | 用 0.25，不阻塞 |
| `H`（日交易总时长h） | `config/trading_hours.csv` 按 `品种.upper()` 查 | **查不到 → 降级 4h + 跳出提醒**（logger 警告一次，`hours_missing` 随看板下发） |

**注意**：
- F 是**品种级**（同品种所有月份共享一个 F 速率）。
- 表键用 `品种.upper()` 直匹配，**不走 `normalize_underlying`**（CFFEX_MAP 会把 MO→IM / IO→IF / HO→IH，与 IM/IF/IH 行撞车）。
- 两端值 `(窗口最低, 窗口最高)` 写入告警记录，与告警值严格一致。
- 旧的 `√T`（剩余到期）口径**已退役**——F 速率不该随期权到期日变化。
- 典型值参考：布朗运动 5min 极差期望 ≈ `1.6σ`，故 ~1.6 为常态，阈值黄 3.0 / 红 5.0 起步，观察后标定。
- 首轮窗口未填满时极差偏小（只会漏报不会误报）。

---

### 3.2 ATM IV 速率（IV Rate）

**用途**：捕捉 IV 突然抬升（卖方 vega 风险）。

**公式**：
```
IV_rate = (H − L) / σ_ref
```
- `H` = 最近 5 分钟窗口内 IV 最大值
- `L` = 最近 5 分钟窗口内 IV 最小值
- `σ_ref` = 该品种 σ_ref（小数形式，与 F 速率/换算Δ 共用同一个数）
- 单位：**%**（前端阈值框里直接填百分号上的数字，预设 5）。值 5.0 = 5min 内 IV 极差达 σ_ref 的 5%
- 量纲换算：`iv` 是百分数、`σ_ref` 是小数 → `(H−L)/(σ_ref×100)` 是无量纲比，`(H−L)/σ_ref` 即百分号上的数字
- 基数**不**另做昨收快照合成（2026-09-21 简化）；σ_ref 缺失 → 该品种跳过

**窗口实现**：
- 内存保留 `_last_iv_samples`：`deque`，每项 `(ts, iv)`，`ts` 为 epoch float。
- 每轮 `_poll_once` 追加当前 ATM IV，并**剔除 > 5min 的过期样本**。
- 窗口内样本数 `< 2` → 跳过（无法形成区间）。
- 服务重启 → `_last_iv_samples` 清空，首轮只写样本不告警。

**清空规则（关键）**：
- **按样本时间间隔判定，不引入交易日概念。**
- 每品种记录上一个样本的时间戳；追加新样本时，若 `新ts − 上一ts > 4 小时` → **判定断档 → 清空窗口重新计数**。
- 否则（间隔 ≤ 4h）→ 连续，保留窗口样本。
- 效果：
  - 日盘午休（~1–1.5h）、商品 10:15–10:30 小节休息（15min）→ 间隔远 < 4h → **连续计数，不清空**；
  - 日盘 → 夜盘（收 15:00 到开 20:00，空档 ~5h）→ **> 4h → 清空**；
  - 夜盘 → 次日日盘（收 02:30 到开 08:00/09:00，空档 ≥5h）→ **> 4h → 清空**。

**ATM 选取规则**：
- 同 (underlying, expiry) 组内，取 `|换算Δ|` 最接近 0.5 的合约（换算Δ = 固定 σ_ref + 该合约自身剩余到期的 Black-76 Δ，不随市场 IV 漂移）；
- 若最接近者 `is_itm=True`，退而取次接近的非 ITM 合约；
- 多个候选取 `iv` 中位数（**观测的仍是市场 IV**，只有选腿用换算Δ）；
- 换算Δ 算不出（缺 K/到期）→ 该候选退回市场 Δ 兜底。

**注意**：
- IV 速率是**品种级**（用主力月 ATM 代表）。
- 窗口数据**不落盘**，只放内存，服务重启清零。

---

### 3.3 Premium Burn（昨日权利金消耗）

**用途**：卖方当日浮亏吃掉昨日收权利金的比例。**衡量"今天赚的钱还在不在"**。

**公式**：
```
Burn = 今日净浮亏 / |昨日净收权利金|
```
- 分子 `今日净浮亏`：`Σ(昨收价 − 现价) × 手数 × 乘数 × dir`，`dir`: 卖=+1、买=−1；**仅计浮亏（>0）**，浮盈不抵扣（保守）。
  - 昨收价 = 快照 `adjust_price`（基准链 §3.7）
- 分母 `|昨日净收权利金|`：`Σ 昨收价 × 手数 × 乘数 × dir` 的**卖方部分**（`dir=+1` 的腿），取绝对值。

**输入**：
| 变量 | 来源 | 缺失处理 |
|------|------|----------|
| 昨收价 | 昨快照 `adjust_price` | 无快照 → 该腿跳过 |
| 现价 | `adjust_price`（模型价，优先）或 `last_price` | 均无 → 跳过 |
| 手数/乘数 | positions_out | — |
| 昨收净权利金 | 同上汇总 | `≤0` → 该品种 Burn 不适用（非卖方）→ 跳过 |

**层级**：**品种级**（`underlying`）。账户级 Burn 另可加（全品种汇总），**本期先做品种级**。

---

### 3.4 Margin 风险度（已有，接入告警）

**公式**（现状口径）：
```
风险度 = 占用保证金 / 动态权益 × 100%
```
- `动态权益 = balance`（CTP 的 balance 已含浮盈浮亏，即动态权益）

**现状**：`_poll_once` L1190-1213 已算好并写 `_shared_state["margin_status"]`。
**本次改动**：不重算，**直接读 `margin_status["level"]` 转成 Alert**。

**注意**：账户级，只有一条。数据缺（`balance<=0`）→ 不告警。

---

## 4. 触发判定

### 4.1 分级

```python
def _level_of(value, warn, danger):
    if danger is not None and value >= danger:
        return "danger"
    if warn is not None and value >= warn:
        return "warn"
    return None          # 未触发
```

**F 速率符号**：取 `|ΔlnF|` 后无方向。**要不要区分涨跌？** → 待确认（见 §8）。

### 4.2 Alert 对象

```python
{
  "alert_id":  "f_rate|IM|warn",     # source|symbol|level（去重键）
  "ts":        "2026-09-21 23:31:02",
  "date":      "20260921",            # 当日计数用（自然日）
  "source":    "f_rate",              # f_rate|iv_rate|burn|conv_delta|margin
  "level":     "warn",                # warn|danger
  "symbol":    "IM",                  # 品种码；账户级为 "ACCOUNT"
  "value":     0.072,
  "threshold": 0.05,
  "msg":       "F速率·黄 IM 0.072 ≥ 0.05"
}
```

**去重键 = `source + symbol + level`**（契约：同一标的、同一触发条件）。

---

## 5. 冷却与计数（后端管）

### 5.1 状态存放

```python
_shared_state["alert_state"] = {
   # key = alert_id
   "f_rate|IM|warn": {
       "date":        "20260921",     # 计数所属自然日
       "count":       2,              # 当日已弹次数
       "last_popup":  1758451862.3,   # 上次弹窗 epoch
       "last_value":  0.072,          # 上轮值（滞回用）
   },
   ...
}
```

### 5.2 弹窗决策（每轮对每个"当前触发"的 alert_id）

```python
def _should_popup(state, alert_id, level, now_ts, today):
    st = state.setdefault(alert_id, {})
    # ① 跨日重置
    if st.get("date") != today:
        st.update(date=today, count=0, last_popup=0.0)
    # ② 当日上限 3 次
    if st["count"] >= 3:
        return False
    # ③ 冷却：黄 10min / 红 5min
    cooldown = 300 if level == "danger" else 600
    if now_ts - st.get("last_popup", 0.0) < cooldown:
        return False
    # ④ 通过 → 计数 + 记录弹窗时刻
    st["count"] += 1
    st["last_popup"] = now_ts
    return True
```

**参数（写死常量，非阈值配置）**：
| 级别 | 冷却 | 当日上限 |
|------|------|----------|
| warn | 600s（10min） | 3 |
| danger | 300s（5min） | 3 |

> 冷却/上限是**行为参数**，不进 `alert_settings.json`（那是数值阈值）。若要可配再说。

### 5.3 表格变色（无冷却、无计数）

后端每轮输出**当前仍触发的行集合**：
```python
_shared_state["active_flags"] = {
   "IM": "danger",        # 该品种当前最高级别
   "AU": "warn",
}
```
- 条件消除 → **当轮即从字典移除** → 前端立即恢复。
- 前端只认现场状态，**不记历史**。

---

## 6. 前端对接（展现形式，需用户确认）

### 6.1 表格变色
- `active_flags[symbol] == "warn"` → 该 L3 行淡黄背景；`"danger"` → 淡红。
- L1/L2 聚合行：任一子合约触发 → 整行同色（取最高级）。
- 用 `tr.classList.add('alert-warn'|'alert-danger')`，CSS 在 `dashboard.html`。

### 6.2 弹窗（Toast）
- 后端返回 `popups: [ {alert_id, msg, level}, ... ]`（仅本轮 `should_popup=True` 的）。
- 前端展示后，调 `POST /api/alerts/ack_popup` 告知已展示？ → **不需要**，后端已在 `_should_popup` 内部计数，**不依赖前端回执**（避免丢包导致重复弹）。
- 同一页面内按 `alert_id` 去重（防止轮询间隙重复）。

### 6.3 历史面板
- `/api/alerts` 或 `/api/dashboard.alerts` 返回 ring buffer。
- 在 `🛡 风控阈值` tab 下加折叠区「告警记录」，倒序 ≤100 条。

---

## 7. 代码改动清单（最小侵入）

| # | 文件 | 位置 | 改动 |
|---|------|------|------|
| 1 | `api_server.py` | `_shared_state` 初始化（L60） | 加 `alerts: []`、`active_flags: {}`、`alert_state: {}`、`_last_F: {}`、`_iv_samples: {}`（每品种 deque） |
| 2 | `api_server.py` | 新增 `_compute_f_rate()` | 品种级 F 速率 |
| 3 | `api_server.py` | 新增 `_compute_iv_rate()` | 品种级 ATM IV 速率（5min 滚动 H/L） |
| 4 | `api_server.py` | 新增 `_compute_burn()` | 品种级权利金消耗 |
| 5 | `api_server.py` | 新增 `_compute_structure()` | 合约级结构风险：单腿 `|换算Δ| ≥ conv_delta_warn` → 黄；该腿越界实值 → 红 |
| 5b | `api_server.py` | 新增 `_conv_delta()` | 换算Δ = 固定 σ_ref + 该合约自身剩余的 Black-76 Δ；缺 F/K/到期 → 不判 |
| 6 | `api_server.py` | 新增 `_evaluate_alerts()` | 汇总五源 → 判级 → 冷却/计数 → 写 alerts/active_flags/active_details/popups；**仅在 `(源,合约)` 级别跳变时写历史** |
| 7 | `api_server.py` | `_poll_once` 末尾 | 调 `_evaluate_alerts(...)` |
| 8 | `api_server.py` | `api_dashboard` | payload 加 `alerts` / `active_flags` / `active_details` / `popups` / `margin_status` / `hours_missing` |
| 9 | `dashboard11.js` | `render()` / `buildL3Row` / `buildAggRow` | L3 行按 `active_details.conv_delta[合约]` 上色；L1/L2 行按 `active_flags[品种]`；单元格级按 `active_details.{f_rate,iv_rate,burn}[品种]` |
| 10 | `dashboard11.js` | `fetchDashboard` 后 | 处理 `popups` → toast（`SRC_LABEL` 中文映射） |
| 11 | `dashboard11.js` | 风控阈值 tab | 告警记录改为**弹窗**（时间倒序/等级筛选/品种筛选，品种按 `underlying`） |
| 12 | `dashboard.html` | `<style>` | `.alert-warn` / `.alert-danger` 行样式 + toast 容器 |
| 13 | `dashboard.html` | 底部 script | 版本号每次改 JS 递增 |

**命名契约（2026-09-21 改名后，勿再漂）**：配置键 `conv_delta_warn`（原 `delta_leg_warn`）；告警源键 `conv_delta`（原 `delta`）；`SRC_LABEL` 含 5 源中文名。理由见设计稿 §一 1.1「两个 Δ 不是一个东西」。

**不新增文件、不新增线程、不新增定时任务、不落盘。**

---

## 8. 边界兜底表

| 场景 | 处理 |
|------|------|
| 无 tick / 无 F | 该品种跳过，不报错 |
| 首轮（无 prev） | 只写 `_last_*`，不判级 |
| `σ_ref` 缺失 | 用 0.25 |
| `T` 极小 | 钳 `max(T, 1/365)` |
| ATM 无 IV | 该品种 IV 速率跳过 |
| IV 窗口样本 <2 | 该品种 IV 速率跳过（首轮/重启后） |
| 非卖方（昨收净权利金 ≤0） | Burn 跳过 |
| 缺 `F`/`K`/到期（换算Δ 算不出） | 结构风险该腿**不判**（不退回市场 Δ，避免口径混用）；仅平值腿选择允许退回市场 Δ 兜底 |
| 单腿 `volume` 缺或为 0 | 结构风险该腿跳过（不除零） |
| Margin 数据缺 | 不告警 |
| CTP 断开 | `_poll_once` 不跑 → 无新告警（自然静默） |
| alert_state 跨日 | `_should_popup` 内检测 `date != today` → 清零 |
| ring buffer 溢出 | `alerts[:-100]` 丢弃 |

---

## 9. 待用户确认（不猜）

> **2026-09-21 状态**：1–6 已全部在实际实现中定案（见下表"结论"列），本文档 §7 为该轮落地后的真实清单。新增待议项在末尾。

| # | 问题 | 结论 |
|---|------|------|
| 1 | F 速率方向：只报幅值，还是分涨/跌两条告警？ | **只报幅值**（`ln(H/L)`，无方向） |
| 2 | IV 速率口径：内部存每分钟 vol，还是直接按 5min 存？ | **存 5min 极差**，口径改为 `(H−L)/σ_ref`，**单位 = %**（百分号上的数字） |
| 3 | Burn 层级：品种级还是兼做账户级？ | **本期只做品种级** |
| 4 | 弹窗是否自动消失？ | **不自动消失**，手动点 × 关闭 |
| 5 | 历史面板位置：折叠区还是独立 tab？ | **改为弹窗**（点弹窗或「查看」打开），非折叠区 |
| 6 | `T` 钳制下限 = 1 天是否合理？ | 换算Δ 改用**该合约自身剩余到期**，F 速率改用**交易时段档位**（`trading_hours.csv`），原 `T` 钳制路径已随旧口径退役 |
| 7 | 阈值输入是否限制范围？ | **不限制**：前端去掉 `min/max/step`，后端去掉 `_clamp_clip`，原值直存 |
| 8 | 品种净 Δ（触发③后半）？ | **本期禁用**：`net_delta_warn=10000`，前端不出输入框，设计预留 |

**新增待议**：品种净 Δ 未来口径（分子含期权腿还是全持仓、锚点是否昨日收盘）、多腿结构（背对/领口）下的净 Δ 定义。

---

*版本：v0.2｜本设计已落地（2026-09-21 更新 §7 清单、§8 兜底、§9 结论）。仍属 v0.2，未升版号——口径未变，只是把实现事实补回文档。*
