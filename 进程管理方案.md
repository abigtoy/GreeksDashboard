建议把系统明确设计成“单实例、单进程、单写者”服务，而不是依赖端口冲突来阻止重复启动。

核心边界：

```text
一个账户/环境
  └─ 一个 DashboardService 进程
       ├─ 唯一 CTP MD/TD 连接
       ├─ 唯一行情/持仓/PnL Worker 线程
       ├─ 唯一快照与结算单写入者
       └─ 唯一 Flask API 监听端口

浏览器、CLI、启动器
  └─ 仅作为 API 客户端，绝不再次创建 Engine 或 Worker
```

建议在设计基线新增“单实例与进程生命周期”章节，包含以下规则。

1. 启动前先申请系统级互斥锁

不要只用 PID 文件或“端口能否绑定”判断。Windows 上使用命名 Mutex 最可靠：

```text
Local\VolTradingDashboard_{environment}_{account_id}
```

- 取得 Mutex：本进程成为唯一服务实例。
- Mutex 已存在：探测既有 `/api/health`，实例健康则退出，不再启动第二套 CTP/Worker。
- Mutex 存在但健康检查失败：记录“疑似残留实例”，由受控恢复流程处理，不要静默再起一个进程。
- 多账户确有并行需求时，Mutex Key 必须包含账户；同一账户永远只能有一个 Engine。

2. API 端口是服务资源，不是抢占资源

固定每个实例的端口并绑定到 `127.0.0.1`，例如：

```text
账户 A → 127.0.0.1:8765
账户 B → 127.0.0.1:8766
```

启动时写入原子 `instance.json`：

```json
{
  "instance_id": "uuid",
  "pid": 12345,
  "started_at": "2026-09-17T10:00:00+08:00",
  "account": "A",
  "port": 8765,
  "status": "running"
}
```

端口被占用时，不要自动换随机端口，否则浏览器、脚本和监控会连到错误实例；应退出并报告占用进程与 `instance.json` 中的实例信息。

3. 禁止 Flask 自动重载与多 Worker

这是 Python 服务意外产生多个进程的最常见来源。正式运行必须规定：

```python
app.run(
    host="127.0.0.1",
    port=8765,
    debug=False,
    use_reloader=False,
)
```

同时禁止：

- Flask `debug=True`；
- Werkzeug reloader；
- Gunicorn/uWSGI 多 worker；
- 在模块 import 时启动 CTP、Worker、定时器；
- 每次 API 请求创建 `VNPYEngine`；
- 断线重连时直接 `subprocess` 起一个新服务。

进程入口必须唯一，并受 `if __name__ == "__main__":` 保护。

4. 将 CTP 与 Worker 设为单写者

只有 `DashboardService` 可以：

- 创建或替换 `VNPYEngine`；
- 发起 CTP 连接和重连；
- 修改 `_trade_cache`、`opened_contracts`、结算缓存；
- 写入快照和 `session_state.json`；
- 发布 `_dashboard_snapshot`。

API 层只读快照。任何“重连”“刷新”“同步结算单”请求都只能投递命令给这个唯一服务进程，不能自行创建子进程或第二个 Engine。

5. 用受控 Supervisor 管理生命周期

建议只保留一种启动方式，例如 `run_server.py` 或 Windows 任务计划程序，不能同时由双击脚本、IDE、批处理、守护脚本各自启动。

生命周期应为：

```text
启动器
  → 获取 Mutex
  → 检查已有实例健康状态
  → 写 instance.json
  → 启动唯一 Engine / Worker / API
  → 心跳更新
  → 正常退出时保存状态、释放 Mutex、删除 instance.json
```

异常退出时，OS 会释放 Mutex；下一次启动仍应通过健康检查和 PID 创建时间核验，避免误杀后来复用同一 PID 的其他程序。

6. 不要自动杀进程

检测到重复实例时，默认动作应是“拒绝启动并展示已有实例信息”，而不是自动 `taskkill`。自动杀进程可能中断持仓、结算单下载或快照写入。可提供受权限保护的“停止当前实例”管理命令，但由用户显式触发。

建议将以下验收条件写入设计：

- 同账户连续执行两次启动命令，只会保留一个进程、一个 CTP 连接、一个端口监听。
- Flask 不因 debug/reloader 派生子进程。
- CTP 断线重连不会创建第二个服务进程。
- 正常空仓、断线、重启和端口被占用时，实例状态均可解释。
- 多账户并行时，仅允许不同账户实例分别持有独立 Mutex、端口、缓存目录和快照目录。