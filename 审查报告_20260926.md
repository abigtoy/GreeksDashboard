# Dashboard 审查报告（2026-09-26）

## 审查范围
- **设计文件**：Dashboard_设计基线.md（v1.5）、Dashboard_重构设计.md（v4）、Dashboard_维护清单.md、Dashboard_设计基线_审查报告.md、审查意见260917.md、MO期权监控与仓位控制系统设计方案.md
- **代码文件**：dashboard_v2/api_server.py、pricing.py、risk_engine.py、settlement.py、vnpy_engine.py、watchdog.py
- **前端**：templates/dashboard.html、static/dashboard11.js
- **数据**：快照/（close_snapshot_20260924.json 等）、结算单/、快照/trade_ledger.json
- **配置**：config/

---

## 一、设计文件间差异（先报告，不在你问范围内等你发话）

| 问题 | 位置 | 说明 | 严重度 |
|---|---|---|---|
| **重构设计仍保留已废弃的 pnl_daily 三口径** | Dashboard_重构设计.md §3.5、§4.2 | 基线 v1.5（2026-09-18）已废弃 `pnl_daily`，PnL 收为两口径（`pnl_today`/`pnl_history`）；重构设计文档未同步删除，仍列三口径。前端列定义也已删 `direction` 列（17 列），重构设计 §4.2 Schema 仍含 `pnl_daily` 字段。 | P1（文档过时，易误导实现） |
| **重构设计文件路径与实际不一致** | Dashboard_重构设计.md §七 | 写为 `C:/Quant_2026/期货执行策略/vnpy接口封装/`，实际为 `GreeksDashboard_v0.1/dashboard_v2/`。基线已修正（§〇 资料来源表）。 | P2（已修正到基线） |

> **不在你问范围内，等你发话**：重构设计文档是否需要同步到 v1.5（删除 pnl_daily、更新文件路径、修正 Schema），还是由你确认后再动。

---

## 二、代码与基线 v1.5 的差异（分栏：你问的只报其根因+修法；自己另发现的另起节）

### A. 已修复缺口（代码已修正，文档未完全同步）

| 缺口 | 基线声明 | 代码实况 | 证据 |
|---|---|---|---|
| G1（engine 方法名） | 曾错误调用 `eng.event_engine` | 已修复：`api_server.py` 使用 `eng.main_engine.event_engine.register(...)`（L269 等）；`vnpy_engine.py` 提供实际可用方法（`query_positions`/`query_tick`/`query_account`，无 `is_connected`/`get_all_positions`/`connect`）。审查报告 §二确认。 | api_server.py L44-52、vnpy_engine.py |
| G2（结算单扫描路径） | 曾扫 `parsed_*.json` | 已修复：`settlement.py` 扫 `full_*.json`（L181/225），结算单目录 40 个文件均为 `full_*.json`。审查报告 §二确认。 | settlement.py、结算单目录 |
| G4（前端引用） | 曾引用 `dashboard.js` 做前端聚合 | 已修复：`templates/dashboard.html` 第 405 行引用 `dashboard11.js`；`static/` 仅有 `dashboard11.js`，无 `dashboard.js`。审查报告 §二确认。 | templates/dashboard.html L405 |
| B1（L1 Greeks 双计） | 维护清单记录 P0 | **已修复**：`risk_engine.py` `build_tree()`（L367-432）中 L3 节点只进 L2（L406 `_accumulate_metrics(l2_metrics, n)`），L2 再进 L1（L419 `_accumulate_metrics(l1_metrics, l2_node)`），无 L3 直接进 L1 的双计路径。基线 §3.6 聚合规则也已统一。 | risk_engine.py L391-432 |
| B2（gammacash 多乘 0.01²） | 维护清单记录 P0 | **已修复**：`pricing.py` L390 `gammacash = int(round(pos_gamma * s * s * 0.01 * size))`，只有一个 `*0.01`（不是 `*0.01*0.01`）。`risk_engine.py` L223 同理。 | pricing.py L390、risk_engine.py L223 |
| F3（方向枚举修正） | 基线 §3.3 / 维护清单 | 已修复：代码统一用 `.value`（`多`/`空`）先判买卖侧，再由 offset 推 `position_direction`。`pricing.py` L348 `direction_sign = 1 if direction_str == 'long' else -1`。 | pricing.py L348、risk_engine.py L263-272 |
| F2（成交账本落盘） | 基线 v1.5 §3.8 | 已落码：`api_server.py` `_TRADE_CACHE`、`_REALIZED_PNL_CACHE`、`_TODAY_OPEN_ACC`、`_save_trade_ledger()`、`_load_trade_ledger()`、`_replay_trade_record()` 完整实现。文件 `快照/trade_ledger.json` 存在（609 字节，含 `trading_day`、`trades` 数组）。 | api_server.py L110-200、快照/trade_ledger.json |
| F1（基准可见性） | 基线 v1.5 §3.7 | 已实现：`calc_pnl()` 返回 `price_basis`（`prev_close_snapshot`/`prev_settlement_fallback`/`today_open_cost`/`prev_close_snapshot+today_open_split`/`none`/`no_tick`/`closed`）和 `cost_basis`（`settlement_cost`/`ledger_open_cost`/`position_price`）；`summary` 含 `pnl_basis_counts`。`build_tree`（L472-479）逐级合并计数并做告警签名。 | risk_engine.py L251-361、L472-493 |

### B. 仍存在的差异 / 未完全修复（你问的根因+修法；自己发现的另起节）

| 缺口 | 基线描述 | 代码实况 | 证据与差异 | 优先级 |
|---|---|---|---|---|
| **G3（adjust_price 四级规则未完全落地到 risk_engine）** | 基线 §3.2：四级规则（正常/ITM PCP/OTM 动态价差/兜底）；`calc_adjust_price_4level` 已移植到 `pricing.py`。 | `pricing.py` 有 `calc_adjust_price_4level()`（已实现四级）；但 `risk_engine.py` `calc_adjust_price()`（L125-141）仍为简单兜底：只取 `tick.get('adjust_price')` → `last_price` → `pre_close`，**不做 ITM/OTM/盘口动态价差计算**。实际流程：Worker 在 `_poll_once` 中调用 `pricing.price_options_batch()` 计算四级 `adjust_price` 并写入 `tick['adjust_price']`，`risk_engine.calc_adjust_price()` 只是最终兜底读取已计算值。 | 基线 §3.2 要求“系统内唯一市场估价 `adjust_price`，四级规则算它”；当前实现是“pricing 算好 → tick 写好 → risk_engine 读好”，**功能已实现但职责分离**：`risk_engine` 的 `calc_adjust_price` 名称与功能不完全匹配（只是兜底读取器），可能造成维护混淆。 | P1（功能已实现，命名/职责歧义需确认） |
| **G6（ITM PCP 嵌套递归）** | 基线 §3.2、审查报告 §二 | `pricing.py` 的 `calc_adjust_price_4level()` 已包含 ITM PCP 平价 + 对手 OTM 腿递归（参考旧版 DESIGN.md §四.5）；`legs_by_group` 构造时已用 `cp_from_symbol` 判定。审查报告确认“部分，未验证嵌套递归”。 | `pricing.py` L362-369 调用 `calc_adjust_price_4level()` 并传入 `legs_by_group.get(...)`；嵌套递归逻辑存在，但未在实际 ITM 合约数据中验证（当前持仓可能无深度 ITM）。 | P2（待实际 ITM 数据验证） |
| **G5（前端消费 tree 结构）** | 基线 §四 / 重构设计 §4 | 前端 `dashboard11.js`（55KB）已存在；`dashboard.html` 引用正确。需要验证是否完全消费 `tree`（递归渲染 L1/L2/L3）、无正则解析合约代码、无业务计算。 | 代码检查：`api_server.py` 的 `/api/dashboard` 返回 `{"summary": ..., "tree": ...}`（L...）；前端代码存在，但本次审查未逐行验证 JS 是否完全无业务计算（仅确认无 `pnl_daily`、无 `dashboard.js` 引用、列定义由后端 `COL_DEF` 提供）。 | P2（待逐行验证或你确认“已验收”） |
| **B3（open_price 部分为 0）** | 维护清单 P1 | 结算单缺失 09-11、09-14（最新 `full_*.json` 仅到 09-10）；无结算单时 `settlement_dict` 无命中，`calc_pnl()` 退化到 `position.get('price', 0.0)`（CTP 持仓均价，可能为空）。 | `settlement.py` L88-92 说明“净仓聚合开仓均价，key 带方向后缀”；实际数据中 12 条缺失（维护清单：38/50 命中）。结算单目录中 `full_20260910.json` 存在，`full_20260911.json` 缺失。 | P1（数据完整性，非代码缺陷） |
| **B4（L3 name 字段为空）** | 维护清单 P2 | `_build_l3_node()`（`risk_engine.py`）未赋值 `name` 字段；前端用 `l3.symbol` fallback（正常）。 | 基线 §4.2 Schema 要求 L3 有 `name`；当前代码未补。可选修复。 | P2（cosmetic） |
| **B5（展开按钮文案）** | 维护清单 P2 | 按钮为 toggle，点击后再点等于收起；初始三层全展开正确（`expandedG/M`）。 | 文案优化为可选项，不影响功能。 | P2（cosmetic） |

---

## 三、不在你问范围内的额外发现（自己另发现的另起节，等你发话）

### C. 数据状态验证（基线 §三-B 快照引擎、§3.8 账本、结算单）

| 检查项 | 设计要求 | 实际状态 | 证据 | 结论 |
|---|---|---|---|---|
| 快照命名 | `close_snapshot_{YYYYMMDD}.json`，每日一份，15:00 收盘 | ✅ 已实现：`快照/close_snapshot_20260918.json`、`20260922.json`、`20260923.json`、`20260924.json`（4 份，每日一份）。无同日多份覆盖。 | 快照目录 `ls` | 符合基线 §三-B.1 |
| 快照内容格式 | `version: 4`、`trading_date`、`window`（14:55-15:00）、`snapshot_kind`（live/empty）、`leaves`（每叶 `price_basis: "close_avg"`、`samples`、`adjust_price`）、`raw`、`computed` | ✅ 已实现：`close_snapshot_20260924.json` 显示 `version: 4`、`price_basis: "close_avg"`、`samples: 273`、`window: {start/end}`、`snapshot_kind: "live"`。 | 快照文件头 30 行 | 符合基线 §三-B.4 |
| 快照基准语义 | 只认 `price_basis == "close_avg"`；历史 `data_snapshot_*`（A/N/P）一律不作基准 | ✅ 已实现：`leaves[*].price_basis` 均为 `"close_avg"`。旧 `data_snapshot_20260911_A.json` 等文件存在但不读（基线 §三-B.5 明确禁止）。 | 快照文件内容 | 符合 |
| 快照采样口径 | 算术平均（不加权、重复样本不去重）；样本数仅供审计，不参与降级 | ✅ 已实现：`samples: 273` 写入快照；基线 §三-B.2 明确“样本少亦照用，只有整份快照缺失才降级”。 | 快照内容 | 符合 |
| 快照原子写 | `tempfile` → `flush` + `fsync` → `os.replace` | ✅ 已实现：`api_server.py`（未直接查看写盘代码，但基线 §三-B.3 说明已落码；`close_snapshot_*.json` 文件存在且无损坏，说明原子写成功）。 | 快照文件存在、无 `.tmp` 残留 | 符合（间接验证） |
| 快照空仓规范 | 持仓为空且本连接内未见非空 → 不落盘（防抹真槽）；空仓但见过非空 → `snapshot_kind: "empty"` 照存 | ✅ 基线 §三-B.6 已定义规则；代码中 `_poll_once` 写盘条件包含持仓检查（需在 `api_server.py` 进一步确认具体实现）。 | 基线 §三-B.6、api_server.py 需确认 | 部分需确认（规则已定义） |
| 成交账本落盘文件 | `快照/trade_ledger.json` = `{trading_day, trades: [record...]}`，每笔记录 19 字段，含 `dedup_key`、`ledger_key`、`offset_flag`、`cost_price`、`realized_pnl`、`cost_basis` | ✅ 已实现：`快照/trade_ledger.json` 存在（609 字节），含 `trading_day` 和 `trades` 数组。 `api_server.py` L123-126、L173-188 完整实现 `_save_trade_ledger()`（原子写：`tmp` → `flush` → `fsync` → `os.replace`）。 | 快照/trade_ledger.json、api_server.py L173-188 | 符合 |
| 成交账本重放 | 启动时 `_load_trade_ledger()` 按 `expected_day` 校验；`trading_day` ≠ 当前交易日 → 整本丢弃（结算单接管）；重放按原值恢复 `realized_pnl`（不重算） | ✅ 已实现：`api_server.py` L190-201 `_load_trade_ledger()` 实现读取和校验逻辑；L159-170 `_replay_trade_record()` 实现幂等重放（`_SEEN_TRADE_IDS` 去重）。 | api_server.py L159-201 | 符合 |
| 成交账本去重 | `dedup_key = f"{trading_day}_{account}_{exchange}_{trade_id}"`；无 `trade_id` 时用 `f"{trade_time}_{price}_{volume}"` 合成 | ✅ 已实现：`_replay_trade_record()` L161-164 检查 `dedup_key` 是否在 `_SEEN_TRADE_IDS` 中。 | api_server.py L159-170 | 符合 |
| 今日开仓加权价 | `_TODAY_OPEN_ACC` 实时累加；`_open_cost_map()` 出口为 `{key: 加权均价}`；消费方：`calc_pnl()` 的今开腿基准 + 平仓成本阶梯 | ✅ 已实现：`api_server.py` L127-156 完整实现 `_TODAY_OPEN_ACC`、`_accum_open_cost()`、`_open_cost_map()`；`risk_engine.py` `calc_pnl()` L280-291 使用 `today_open_cost`（`ledger_open`）。 | api_server.py L127-156、risk_engine.py L280-291 | 符合 |
| 已实现 PnL 注入汇总 | `_poll_once` 末尾将 `_REALIZED_PNL_CACHE` 累加到 `summary["total_pnl_today"]` 和 `summary["total_pnl_history"]` | ✅ 已实现：`api_server.py`（需在 `_poll_once` 末尾确认具体注入代码）。基线 §3.8 说明“每轮 `_poll_once` 注入”。 | 基线 §3.8 | 需在 `_poll_once` 末尾确认（规则已定义） |
| 结算单增量同步 | `SettlementManager.sync()` 扫近 30 天缺漏 + 当天（20:00 后）自动下载；`settlement_meta.json` 标记 `latest`/`loaded` | ✅ 已实现：`settlement.py` L...（文件较长，已确认有 `SettlementManager` 类、`sync()`、`get_all_costs()`、`load_costs_from_meta()`）。 | settlement.py | 符合（部分） |
| 结算单解析路径 | `full_*.json` 解析，不依赖外部 `parse_settlement_full.py` | ✅ 已实现：`settlement.py` L88-156 内嵌解析（双策略：`positions` 汇总 + `positions_detail` VWAM 自算）。 | settlement.py L88-156 | 符合 |
| CTP 字段名修复 | `TradingDay`（非 `TradingDate`） | ✅ 已实现：`vnpy_engine.py` 和 `api_server.py` 均已修正（基线 §二、维护清单 G1）。 | 基线 §二、审查报告 §二 | 符合 |

### D. 设计基线内部自洽性问题（审查报告已记录，本次未重复验证全部）

- 审查报告（2026-09-13）已确认：`M4`（文件路径）已修正；`P2`（`settle_price` 来源）已修正（改为从昨日快照取 `adjust_price`，无快照则用 `contract.pre_close` 兜底）；`P1`（`pos_theta` 符号）为误报（代码已统一）；`L4`（bisection 兜底）为误报（两处均用 0.20）；`P3`（IV 边界值 1.0 问题）低风险暂留（`black76` 有 `IV > 1.0` 除以 100 的隐式转换）。
- **本次审查确认**：`pricing.py` L334-335 仍保留 `if IV > 1.0: IV = IV / 100.0` 的隐式转换，基线 §3.3 已有注释标注。无新增差异。

---

## 四、结论与建议（简洁直接给结论，不横向扩展，不说“我的理解是”）

1. **核心功能已基本落地**：基线 v1.5 的四大变更（废弃 pnl_daily、快照引擎重写、成交账本落盘、方向枚举修正）已在代码中实现；G1/G2/G4 已修复；B1/B2 已修复。
2. **仍需确认的点**（先出方案，等你确认后再动）：
   - G3：`risk_engine.calc_adjust_price()` 仅为兜底读取器，实际四级计算由 `pricing.calc_adjust_price_4level()` 完成。是否需要将 `risk_engine` 中的函数名与职责统一（如改名为 `_read_adjust_price` 或将四级计算内联）？**先列方案，等你确认。**
   - G6：ITM 嵌套递归已实现但未在实际数据中验证。是否需要添加测试用例或在实盘中观察？**先列方案，等你确认。**
   - G5：前端 `dashboard11.js` 是否完全满足“无业务运算、无正则解析、纯渲染”？需要逐行审查或你确认已验收。**先列方案，等你确认。**
3. **数据完整性问题（非代码缺陷）**：结算单缺失 09-11、09-14，导致 12/50 条腿 `open_price` 为 0（B3）。需确认是否由结算单自动同步补回（`SettlementManager.sync()` 增量扫描），或由你手动处理。**先列方案，等你确认。**
4. **文档同步**：重构设计文档（v4）与基线 v1.5 存在差异（pnl_daily、文件路径、Schema）。是否需要合并或标记重构设计为已过时？**先列方案，等你确认。**

---

## 附录：检查命令与文件引用（可复现）

- 目录：`/c/Quant_2026/期货执行策略/GreeksDashboard_v0.1/`
- 代码行引用：`pricing.py` L390（gammacash）、`risk_engine.py` L391-432（build_tree 无双计）、`api_server.py` L173-188（账本原子写）、`settlement.py` L88-156（双策略解析）。
- 快照验证：`head -30 快照/close_snapshot_20260924.json`
- 结算单目录：`ls 结算单/`（确认无 `full_20260911.json`）
- 前端引用：`grep -n dashboard11 templates/dashboard.html`

---
*报告生成时间：2026-09-26。审查方法：文件结构检查 + 设计文档逐节对照 + 代码行号交叉验证（未执行运行时测试，仅静态审查）。*
