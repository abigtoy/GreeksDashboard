# 《Dashboard_设计基线》合并审查意见

范围：本文仅审查设计文档中的规则、伪代码与接口契约一致性；未验证引用代码是否已按文档实现。

## P0 — 必须在实现/上线前修复

| 编号 | 问题 | 影响与修正建议 |
|---|---|---|
| P0-1 | §3.7 的 PnL 公式遗漏 `direction_sign` | 四分法及混合仓的已实现/未实现公式均未统一带多空符号，空头会被按多头计算。统一定义 `signed_qty = direction_sign * abs(volume)`；所有 PnL 分项以“原持仓方向”乘符号，不能以平仓买卖方向替代。 |
| P0-2 | 混合仓拆分不守恒，且已实现 PnL 使用错误手数 | `vol_老 = current - today_open`、`vol_新 = today_open - today_close` 会重复处理平仓量；`pnl_新_realized` 又使用剩余数量而非已平数量。应先基于昨仓初始量、今开量、平仓量逐项分配，保证“老仓剩余 + 今仓剩余 = 当前仓位”。 |
| P0-3 | 不能将“FIFO 优先平昨”作为唯一修复规则 | 应优先保存并使用 CTP 的开平标志、平今/平昨标志、账户、交易所、方向与成交 ID。只有字段缺失时才使用明确、可审计的降级分配规则；否则会与真实持仓归因不一致。 |
| P0-4 | 结算成本字典取值丢失方向维度 | §2 的 Key 是 `{symbol}_{多/空}`，而 §3.7 伪代码使用 `settlement_dict.get(sym)`；将导致 VWAP 成本全线未命中并退化为错误成本。必须统一为 `settlement_dict.get(f"{sym}_{direction_str}")`。 |
| P0-5 | 成交缓存缺少完整身份和幂等去重 | 缓存仅按 `symbol`，不能隔离多空、账户、交易所；重连或恢复时可能重复处理成交。缓存/去重键至少应包含 `TradingDay + Account + ExchangeID + TradeID`，归因键还应包括 `symbol + position_direction + offset`。 |
| P0-6 | 已完全平仓合约没有 PnL 输出通道 | `calc_pnl(position, ...)` 仅处理当前持仓；全平合约没有 `position`，但其已实现 PnL 仍应进入当日与历史汇总。应建立独立的已实现 PnL 聚合，再与持仓浮动 PnL 合并。 |
| P0-7 | 期权缺标的价时错误把期权价格作为 Black-76 的 `F` | `underlying_price` 缺失时回退 `last_price`，对期权会把权利金当期货价，使 IV、Greeks、现金敞口失真。必须经合约映射取得对应期货的有效行情；缺失时标为不可计算或陈旧，不得用期权价替代。 |
| P0-8 | 空持仓不落盘会产生“幽灵持仓” | 正常查询返回空持仓时仍保留旧快照，重启后可能恢复已平仓风险敞口。仅在查询失败、断连等异常状态下保护旧快照；正常返回空列表必须原子写入空状态。 |
| P0-9 | 已知 B1/B2 仍为待修 | B1 会使 L1 Greeks 重复累加为两倍；B2 会使 Gamma cash 少 100 倍。二者不能继续作为基线遗留项，应完成代码修复、回归测试后才能关闭。 |

## P1 — 高风险逻辑、口径或可靠性问题

| 编号 | 问题 | 建议 |
|---|---|---|
| P1-1 | §3.5 与 §3.7 同时存在两套不兼容的 `calc_pnl` 规范 | §3.7 虽称替代旧版，但旧实现仍完整保留，参数签名、成本来源和逻辑均不同。删除旧规范或明确标为废弃，保留唯一接口。 |
| P1-2 | 结算成本“双策略兜底”与代码不一致 | 明细 VWAP 会无条件覆盖汇总均价，不是兜底；未知或空方向还会静默归为空头。应只在汇总值缺失时用明细，并对未知方向拒绝入账、告警。 |
| P1-3 | IV 单位不统一 | 二分法返回小数，如 `0.165`；Schema 示例使用百分数，如 `16.5`；模型以 `IV > 1` 猜测单位。内部统一使用小数 `iv_decimal`，展示层单独输出百分比。 |
| P1-4 | IV 反推缺少无套利与行情质量校验 | 仅校验价格大于零，未校验 Call/Put 理论上下界、bid/ask 合法性、时间戳和陈旧行情。失败时不能静默给出可显示 IV，应带 `invalid/stale/fallback` 状态与来源。 |
| P1-5 | 到期时间下限冲突 | §3.2 使用 `0.5/250`，§3.3/§3.4 使用 `0.5/365`。必须统一年化口径并提取为常量，配套到期日回归测试。 |
| P1-6 | “无 Tick 则 pnl_today=0”缺少估值状态 | 结果可能与用昨日 Mark 估值相同，但“尚未开盘”“行情陈旧”“真实 PnL 为零”不能共用数值零。应输出 `not_opened`、`stale`、`valid` 等状态。 |
| P1-7 | 快照写入不具备崩溃安全性，且没有收盘强制保存 | 覆盖写可能在异常退出时损坏最后快照。应采用临时文件、`fsync`、原子替换；除 30 分钟定时外，在各品种会话收盘、断连前和服务退出时强制保存。 |
| P1-8 | 快照日期和会话窗口未绑定交易日历 | 快照文件日期必须取 CTP `TradingDay`，不可取本机自然日；夜盘结束时间因品种而异，不能统一写死为 02:30。应由交易所/品种日历驱动。 |
| P1-9 | 内存“不可变 Atomic Swap”定义不足 | 仅替换顶层引用不足以保证嵌套 `dict/list` 不被后续写入。构建完成后应深拷贝/冻结，再发布快照，并规定所有读者只读。 |
| P1-10 | CTP API 端点缺少部署安全约束 | `/api/ctp/connect`、`disconnect` 未规定仅本机访问、鉴权、CSRF、审计和限流。若服务不严格绑定 loopback，会产生连接控制风险。 |
| P1-11 | 缺少输入有效性校验 | `F`、`K`、到期日、乘数、成交价、数量为 0、空值或异常字符串时可能造成数学异常、错误回退或静默零值。应定义“拒绝计算、保留上次、告警”的统一策略。 |
| P1-12 | trade cache 持久化说明自相矛盾 | 文档一处标记成交缓存可持久化，另一处又说重启后今仓基准丢失只能归零。必须明确：完整恢复、重新查询重建、或受控降级，三者只能有一个正式行为。 |

## P2 — 规范、可维护性与验收问题

| 编号 | 问题 | 建议 |
|---|---|---|
| P2-1 | §3.2 标题称“三级规则”，正文列出四级规则 | 统一术语，避免实现人员遗漏第四级兜底。 |
| P2-2 | 文件结构与版本说明冲突 | §七与§十二的目录层级、基线版本标识不同。指定唯一权威目录和版本，其他内容链接或归档。 |
| P2-3 | 阈值缺少账户、币种、乘数和版本维度 | 阈值应配置化、可按账户/策略覆盖，并记录版本与生效时间，避免事后无法解释颜色标签。 |
| P2-4 | PnL/快照没有明确验收样例 | 将多空、今开今平、平昨/平今、混合仓、全平仓、重连重放、缺结算单、缺行情、夜盘跨日、正常清仓等场景写成固定输入输出测试。 |

## 建议的修复顺序

1. 先统一仓位与成交账本模型：方向、账户、交易所、开平标志、成交去重与持仓守恒。
2. 重写 §3.7：所有 PnL 统一带方向，拆分老仓/今仓，纳入已完全平仓的已实现 PnL。
3. 修复 B1、B2、结算成本 Key 与期权标的价格链路。
4. 修复快照：真实空仓落盘、原子文件写、CTP `TradingDay` 命名、收盘强制保存。
5. 最后收敛 IV 口径、异常状态、接口安全和文档版本。

在完成前四步及对应测试前，不建议将该基线作为自动生成实现代码或用于真实风险/PnL判断的唯一依据。





=================================================================================================================
=================================================================================================================


整体确认：这份清单很接近可直接进入文档修订的版本。P0-1、4、6、7、8、9、10 的问题判断成立；但 P0-2、3、5、6、7、8 以及若干 P1 的“修改代码”需要调整，避免引入新问题。

## P0 逐条确认

| 项目 | 结论 | 修改意见 |
|---|---|---|
| P0-1 方向符号缺失 | 确认 | 所有 PnL 分项应使用 `position_sign * (price - base) * abs(qty) * size`。这里的 `position_sign` 是原持仓方向；平多的卖出成交不应被误认为空头方向。 |
| P0-2 混合仓不守恒 | 确认，但修复不能只依赖 FIFO | 你的数量守恒公式在“确实按先平老仓”时正确。正式规则应优先使用 CTP 的 `CloseToday/CloseYesterday`；只有缺失时才按 FIFO 降级。平仓成交还必须按“平老/平新”分别计算成交量加权平仓价，不能共用一个 `P_trade_close_avg`。 |
| P0-3 FIFO 降级 | 确认 | 建议补充：`OffsetFlag` 是第一优先级；FIFO 仅为缺失时的可审计降级。降级结果需带 `allocation_source="ctp_offset" / "fifo_fallback"`。 |
| P0-4 成本 Key 缺方向 | 确认 | 改为 `settlement_dict.get(f"{sym}_{direction_str}", ...)`。同时确保 `sym` 已剥离交易所后缀、`direction_str` 只能是经过校验的“多/空”。 |
| P0-5 trade cache 去重 | 确认，但缓存 Key 需改 | 不建议把 `offset` 放进主缓存 Key，否则开仓与平仓记录会被割裂。建议账本分组 Key 为 `(trading_day, account, exchange, symbol, position_direction)`；每条成交保留 `offset`。去重 Key 使用 `(trading_day, account, exchange, trade_id)`。注意：平仓成交的买卖方向与原持仓方向相反，必须转换出 `position_direction`。 |
| P0-6 全平合约 PnL 丢失 | 确认，但示例会双计 | 不能直接把整个 `_realized_pnl_cache` 再加到 `summary`，否则部分平仓、仍有持仓的合约可能被重复累计。应建立统一 PnL 分类账：每笔已实现 PnL 只入账一次；当前持仓只贡献未实现部分；全平合约的已实现部分仍进入汇总。 |
| P0-7 期权误用自身价格作 F | 确认，但 proposed code 违反设计红线 | `calc_greeks`/`pricing.py` 被要求为纯函数，不能在其中调用 `engine.query_tick()`；且示例中的 `pos_list`、`und` 未定义。应由 Worker 完成标的映射和行情查询，将 `underlying_price`、`underlying_source`、`underlying_ts` 显式传入纯函数。缺失时返回完整的“不可估值节点”，而非异常形状的 `{"error": ...}`。 |
| P0-8 空仓幽灵快照 | 确认，但不要另建 `_empty` 文件 | 应使用同一个规范快照文件原子覆盖，写入 `query_status: "ok"`、`positions: []`、`tree: []`、`snapshot_kind: "empty"`。另建 `_empty` 文件会留下旧的正常快照，并让恢复逻辑产生双源歧义。 |
| P0-9 B1/B2 待修 | 确认 | 状态应先改为“修复中/待数值验证”，不能直接写 CLOSED。B2 的正确影响是 Gamma cash 被缩小 100 倍。 |
| P0-10 双策略成本覆盖 | 确认，但应增加差异审计 | 仅在汇总均价缺失时由明细 VWAP 兜底，与你的修改一致。若两者同时存在且差异超过容忍阈值，应告警并记录来源；不要静默相信任一来源。未知方向必须拒绝入账。 |

## P1 逐条确认

| 项目 | 结论 | 修改意见 |
|---|---|---|
| P1-1 两套 `calc_pnl` 并存 | 确认 | 不建议只加 `DEPRECATED`。应删除旧实现正文，保留一句“已由 §3.7 替代”，并只保留一个正式函数签名。 |
| P1-3 IV 单位猜测 | 确认，但入口也不能猜测 | `iv > 1` 的判断只是把猜测移动到入口。内部统一 `iv_decimal`；迁移旧数据时必须由字段名或显式 `iv_unit` 决定换算，未知单位拒绝计算。 |
| P1-5 T 下限冲突 | 确认 | 若 `days_to_expiry` 是自然日，则统一 `0.5 / 365.0`；若是交易日，则统一使用 `/250`。关键是字段定义与公式必须成对统一。 |
| P1-6 无 Tick 状态缺失 | 确认 | 增加 `market_status` 合理。建议使用 `valid`、`not_opened`、`stale`、`invalid_underlying`，并携带 `price_source` 与最后行情时间。 |
| P1-7 快照非原子写 | 确认，但给出的 Windows 示例需调整 | Windows 下 `NamedTemporaryFile` 仍打开时调用 `os.replace()` 可能失败。应在同目录创建临时文件、写入并 `fsync`、关闭文件后，再调用 `os.replace(tmp_path, target_path)`。 |
| P1-8 快照日期 | 确认，但不要假设 `engine.get_trading_day()` 存在 | 文档已说明引擎可用接口有限。应把 CTP `TradingDay` 作为 Worker 已维护的状态或从有效 Tick/回报中传入；禁止以本机日期作为正常回退。 |
| P1-9 Atomic Swap | 确认，但深拷贝不是完整定义 | `copy.deepcopy` 可作为发布边界的一步；还要规定“发布后禁止任何写入”，并确保 Worker 的下一轮计算不复用已发布嵌套对象。 |
| P1-11 输入校验 | 确认，但不应只返回错误 dict | `F <= 0`、`K <= 0`、无效到期日应返回字段完整的节点，指标可为 `null`，并附 `calc_status`；否则树聚合和前端 Schema 会被破坏。`volume == 0` 通常应在进入树前过滤。 |

## 还应补入的 P1

- **IV 反推缺少无套利边界和行情时效校验**：应校验 Call/Put 理论上下界、bid/ask 合法性、行情时间戳；ATM IV 回退必须标注来源。
- **快照缺少会话收盘强制保存**：除定时保存外，应在各品种的实际收盘、正常退出、连接状态切换时强制保存。
- **交易日历与会话时间未按品种定义**：文件日期必须绑定 `TradingDay`，夜盘结束时间不能统一假设为 02:30。
- **CTP 控制 API 缺安全约束**：明确仅绑定 loopback，或增加鉴权、CSRF、防重放、审计与限流。
- **成交缓存“持久化”与“重启后今仓成本归零”矛盾**：明确唯一恢复策略：完整恢复、重新查询重建，或受控降级。
- **结算下载缺少版本切换规则**：下载不完整、解析失败、成本文件不一致时，不能将半成品写入当前生效缓存。

## P2 逐条确认

| 项目 | 结论 | 修改意见 |
|---|---|---|
| P2-1 四级规则标题 | 确认 | 改为“adjust_price 四级规则”。 |
| P2-2 两份文件结构 | 确认，但不建议直接删除历史章节 | 保留一份作为唯一“当前文件结构”，另一份改为简短的历史变更说明或删除重复内容。版本号应在实际修订完成后再升至 v1.3。 |
| P2-3 阈值无版本/账户维度 | 确认 | `thresholds.json` 可行；建议包含 `account_scope`、`product_scope`、`currency`、`version`、`effective_from` 与变更审计信息。 |

建议把以上内容转为基线的“修订决议”：先冻结 P0 的账本、方向、成交归因和快照语义，再实现 PnL 与前端展示。这样能避免先修公式、后发现成交归因基础不成立而再次返工。