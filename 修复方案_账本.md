# 修复方案：_on_trade 真实交易入账（SC/RU 当日盈亏缺口 7000）

## 根因（已确认）
- `_on_trade` 回调只写入一条坏条（`symbol=''`、`volume=0`、`offset=close_yesterday`、`cost=0`、`realized=0`）
- 真实 `3 ru + 3 sc`（共 6 笔）未入 `_TRADE_CACHE` / `_REALIZED_PNL_CACHE`
- 结果：`pnl_today` 缺已实现盈亏（差约 `7000`）
- 结算格式、`settle_key`、已实现注入、重启已修；剩余缺口 = 账本数据缺失

## 设计（不改 `calc_pnl` 基准链）
### 1. 修 `_on_trade` 解析（阻止坏条写入）
- 现状：`symbol=''` 时仍写入 `_TRADE_CACHE`（无过滤）
- 修：`if not symbol or not volume or volume <= 0` → 直接 `return`，不写账本、不写 `_REALIZED_PNL_CACHE`
- 同时检查 `raw_dir` 解析：`dir_val = getattr(..., 'value', '')` 对枚举值应正确取 `'多'`/`'空'`，但 `raw_offset` 解析若无匹配则 `fifo_fallback` 可能误判

### 2. 确保真实交易入账
- 检查 `_register_trade_event`：确认 `EVENT_TRADE` 已注册到 `eng.main_engine.event_engine`
- 检查 `raw_offset` 解析：`平`（无今昨）→ 当前代码默认 `close_yesterday`，这对中金所等无今昨区分品种正确，但需确认 `SC`（能源中心）期权的 `offset` 值格式
- 检查 `position_direction` 映射：`trade_side`（买卖）与 `offset_flag`（开/平）的反向关系是否正确

### 3. 清理当前污染状态
- 运行前：删除坏条（`trade_ledger.json` 清空）
- 运行前：清 `_TRADE_CACHE`、`_SEEN_TRADE_IDS`、`_REALIZED_PNL_CACHE`、`_TODAY_OPEN_ACC`（内存重置）
- 重启后：验证 `_TRADE_CACHE` 有真实条目（`sym` 非空、`volume > 0`）

### 4. 验证标准（修完即测）
- 重启后 `api/dashboard` 中 `sc2611P650` 的 `pnl_today` 应包含已实现（不再缺 `7000` 左右）
- `trade_ledger.json` 应有 `sym='sc...'` 且 `volume > 0` 的真实条目
- `pnl_basis_counts` 无异常（`prev_close_snapshot` 正常计数）

## 不做（已确认正确，禁动）
- `pricing.py` `settlement_data` 格式（已修）
- `api_server.py` `settle_key` 英文化（已修）
- `calc_pnl` 基准链（已确认正确：昨快照 → 结算价 → 今开价）
- 已实现注入逻辑（已修：`_poll_once` 注入 `_REALIZED_PNL_CACHE` 到叶子节点）
- `SC` 乘数 = `1000`（用户确认，不是 `100`）

## 执行顺序（等批准后）
1. 修 `_on_trade` 过滤（阻止坏条写入）
2. 重置内存账本（清 `_TRADE_CACHE` 等）
3. 重启服务（清文件 + 重启进程）
4. 验证：`api/dashboard` 显示 SC 盈亏包含已实现，账本有真实条目
