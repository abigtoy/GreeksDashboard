# 监控预警 设计稿 v0.1（讨论中，未批准，未落码）

日期：2026-09-19
状态：**草案**。本文只汇总"行情异动 → 希腊字头寸失控"预警的讨论结论与待决问题。
基线文档 `Dashboard_设计基线.md` **未改动**，待方案定稿后再写入。

---

## 0. 目标

及时提醒**异常波动**导致的各希腊字头寸失控。范围：**整体（品种级）头寸**为主，**合约头寸**保留到哪个粒度待定（见 Q3）。

用户明确要求：**不只是显示，要预警 + 控制**。"控制"具体含义未定（见 Q4）。

---

## 1. 已定案事项

| # | 结论 |
|---|---|
| 1 | **全部按品种监控**（AU/CU/…分别计算，不做全账户混合） |
| 2 | 四象限是**两张图**，不是速率×幅度交叉：图1 = **速率**，图2 = **幅度**。每张图 **X 轴 = 标的变动，Y 轴 = IV 变动**，两变量同图对照（单位不同 → 双 Y 轴或对数归一） |
| 3 | 图的用途 = 观察标的与 IV **共振 / 背离 / 无关**，不是用来画阈值线报警 |
| 4 | **共振/背离的前提是"两个都有特征"**（都超阈值）。只有一个超阈值 或 两个都不超 → 一律归"**无关**" |
| 5 | **长窗幅度基准 = 昨收**。告警本身不一定是坏事，阈值靠经验标定（不拍数字） |
| 6 | **短窗 = 5 分钟**（速率窗口） |
| 7 | **持续性只决定"消除"，不决定"触发"**。捕捉到即报，零延迟，宁滥勿缺 |
| 8 | **ITM 风险公式**：`|K−F|/F < 0.10 × σ × √(20/365)`，其中 σ 直接取年化小数（0.20 = 20%）。`√(20/365)` 为**固定常数 0.234**（20–30 天到期的经验值反算），不随各合约真实剩余期限变化。T=20 天 |
| 9 | **净买方不管**：净支出权利金的组合不触发盈亏告警 |
| 10 | **盈亏告警口径**：净收取权利金按**上日收盘持仓**算（静态基准，不随当日起平仓变化）；当日浮动盈亏累计（品种级，含期货腿对冲部分，含手续费）≥ **净收取权利金 × 20%** 即提醒 |

---

## 2. 量纲对齐（关键）

标的与 IV 性质不同：`F` 是真数轴、涨跌有天然符号；`σ` 恒正、只有扩张/收缩，单看它"只有一半信息"，另半截要借标的方向才成立。

**对齐办法**：两者都取**对数变化量**，统一为有符号、无量纲。

- `d(lnF) = ΔF / F`
- `d(lnσ) = Δσ / σ`（σ > 0 合法）
- IV 方向定义：`d(lnσ) > 0` = 扩张

方向组合的语义（只在"两个都有特征"时有意义）：

- `F↓ σ↑` → **恐慌背离**（卖 strangle / 卖 put 的雷，gamma + vega 双杀）← 最该盯
- `F↑ σ↑` → 逼空共振（卖 call 的雷）
- `F↑ σ↓` → 温和上行
- `F↓ σ↓` → 有序下跌

---

## 3. 维度清单

基础维度（每品种 × 标的/IV 各一条）：

- **D1 短窗速度**：`|d(lnX)/dt|`，窗口 5 分钟。威胁 = 调仓不及时
- **D2 长窗幅度**：`|ln(X_t / X_昨收)|`。威胁 = 头寸漂移、ITM 化
- **D3 符号**：`sign(d(lnX))`。标的 = 涨/跌；IV = 扩张/收缩
- **D4 持续性**：连续多少个采样回落到阈值内 → 决定告警**解除**（不决定触发）

派生维度：

- **R1 同步性**：标的与 IV 是否**同时**有特征（都超阈值）。否则"无关"
- **R2 方向组合**：见 §2 四语义

---

## 4. 四条告警规则

### 规则 1+2：标的/IV 速率与幅度（观察图）

两张 2×2（速率图、幅度图），标的与 IV 同图。按 §1 表 #4 分类为 共振 / 背离 / 无关。

- 采样周期：标的与 IV 均 **3–5 秒**（与参考腿降频一致）
- 落一个点即一个采样时刻，带尾迹看漂移方向

### 规则 3：ITM 风险（**合约级，逐腿判**）

当月那组**所有在仓腿**逐条判定，条件 ①/② **取先到者**（任一满足即提醒该腿）：

- ① 与平值行权价距离 **≤ 1 档**（"档"= 该品种行权价间距，跨品种不同）
- ② `|K−F|/F < 0.10 × σ × √(20/365)`，σ 用**品种级单一 σ**（非各合约自身反推 IV）

### 规则 4：当日盈亏（品种级）

- 基准：上日收盘持仓的净收取权利金 `net_premium`
- 触发：当日该品种累计盈亏（浮动 + 已实现 + 手续费，含期货腿对冲部分）`pnl_today ≤ −0.20 × net_premium`
- **前提：`net_premium > 0`（净卖方）**。净买方不监控此规则
- `net_premium` 数据源 = **成交账本**（基线 §3.8）

### 规则 5：希腊字头寸失控线（**尚未设计**）

上面 1–4 都是**行情侧触发器**。原始目标里的"各希腊字头寸失控"还需要**头寸侧阈值**——品种级净 Delta / Gamma / Vega / Theta 超线即报。口径未定，见 Q2。

---

## 5. 告警状态机

```
静音 ──[任一采样越阈值]──> 告警(触发) ──[连续 N 采样回落]──> 静音
                              │
                              └──[持续超阈值]──> 保持(不重复刷屏，可升级提示"已持续 X 分钟")
```

- **触发**：单点越线立即报，不等确认
- **解除**：连续 N 个采样在阈值内（防抖）。N 起点 3（≈15 秒）
- **去重**：只在状态**跳变**时推送（静音→告警、告警→静音）。持续中超阈值不重发

---

## 6. IV 基准方案（倾向甲）

- **甲（主力平值）**：直接用已有 `_pick_ref_iv` 输出（同 (标的,到期) 组内 put `K<F` 最大 + call `K>F` 最小 两张的中位数）。一行不用新写，3–5 秒一采
- **乙（VIX 式）**：全链 IV + 到期正态权重做方差互换积分。更稳、抗单腿毛刺，但需订 ITM 腿（**与"ITM 不订"的订阅决策冲突**）、需权重与缺失插值规则，成本明显高一档

**倾向**：甲先上，乙留升级路径。

**甲的已知代价**：平值两腿同时无成交时会退到主力月 / 20% 兜底，此时 `d(lnσ)` 不可信 → 必须给 σ 打 `basis` 标记，**兜底态下冻结 IV 侧告警**（否则假 IV 抖动乱报）。

---

## 7. 阈值标定路径（不拍数字）

1. **阶段 1 观察期**：只落盘不报警。每品种记录 `d(lnF)`、`d(lnσ)`，5 分钟窗 + 昨收长窗
2. **阶段 2**：跑若干交易日，按分位数定阈值（候选：P95 = "有特征"、P99 = "急变"）
3. **阶段 3**：开报警，滞回 N 从 3 起步

---

## 8. 数据前置缺口（实现前必须先解决）

- 现有 `data_snapshot_current.json` **不含**原始 bid/ask/量/时间戳，也没有标的/IV 的时间序列 → 速率类指标（D1）当前无法计算
- 需要 per-underlying 的 `(ts, price)` 环形缓冲（5 分钟 × 3–5 秒 ≈ 60–100 点）
- 需要 per-品种 的 `(ts, σ, σ_basis)` 环形缓冲
- **订阅层尚未实现**：OTM 侧 + 平值全订、不订 ITM、按持仓量/成交量占比阈值选到期月、参考腿降频 3–5 秒。这是本监控的前置依赖
- `option_ticks` 尚未采集 `open_interest`（选流动性月份要用）
- `_poll_once` 仍是逐条 `query_tick` + sleep，需改批量读缓存
- 观察期落盘需要新增文件（jsonl？），**建文件需先批准**

---

## 9. 待决问题清单（下次逐条回答）

**Q1 — IV 基准选甲还是乙？**（我倾向甲 + `basis` 兜底态冻结告警。乙与"不订 ITM"冲突）

**Q2 — 希腊字头寸失控线（规则 5）本轮是否一起定？口径选哪个：**
- 绝对金额（如净 Vega ≥ X 元 / 1% vol）
- 占保证金比例
- 占名义本金比例
- 或分品种预算（每品种给一个 vol 预算池）

**Q3 — 合约粒度保留到哪些规则？** 现假设：只有 **ITM 风险（规则 3）** 落到合约级，其余全部收到品种级。对不对？最初目标提到"合约头寸"，是否还需要**单腿希腊字失控**？

**Q4 — "控制"是什么含义？**
- 只提醒（前端 + 推送）
- 提醒 + 限制开仓（禁止该品种新开仓，只允许减仓）
- 提醒 + 推荐对冲量（给出把净敞口拉回阈值内所需的成交量）

**Q5 — 告警送到哪？** 前端变色/弹窗、Telegram 推送、本地日志。三态跳变才发，量不大。是否需要声音/静默时段？

**Q6 — 速率单位统一问题。** 早前我写过"元/分钟"，跨品种不可比。是否统一改为 `d(lnF)/dt` = **%/分钟**？（幅度同理用 %）

**Q7 — ITM 风险"档"的定义。** "1 档" = 该品种行权价间距？同一标的不同月份间距不同时怎么处理？

**Q8 — σ 的 `basis` 分级。** 参考 IV 来源分四级（平值成交 / 主力月 / 20% 兜底 / 模型价夹盘口）。哪几级**允许**触发 IV 告警，哪几级**冻结**？

**Q9 — `net_premium` 口径细节。**
- 只算期权腿的权利金，期货腿不计入基准（但计入当日盈亏）——对吗？
- 上日收盘持仓若含已到期/已平掉的腿，如何取？（取上日快照那一刻的持仓账本）

**Q10 — 非交易时段与切日。** 集合竞价、午休、夜盘→日盘切换、夜盘跨 0 点：
- 5 分钟窗口是否跨非交易时段连续计算（午休 11:30–13:30 应切断，不然下午第一个点速率会算错）
- 夜盘属于哪个业务日（须按 `TradingDay`，不按本机时钟）
- 收盘后是否自动解除全部告警

**Q11 — 数据陈旧时的行为。** 标的 tick 陈旧（`_is_stale` 整条作废）→ F 侧告警是否也冻结？品种无任何持仓时是否跳过监控？

**Q12 — 观察期时长与落盘格式。** 跑几天？字段清单？（阶段 1 只记录不报警）

**Q13 — 阈值是否分品种？** 各品种波动率量级差异大（AU vs CU vs SC），统一分位数 or 每品种各标一套？

---

## 10. 未做/搁置

- **settlement sync 两个 bug 未修**（`_local_complete` 一票否决 + sync 只在连接成功触发一次），方案已备好待批准。影响 `pnl_today` 基准，与规则 4 直接相关
- 基线 §3.7（快照为基准、结算单为备用）本轮已落码修复 `_prev_trading_day_file`；结算单下载 20:20 时间门待落码
- 定价层 `pricing.py` 已改完并自测 7/7 PASS，**未重启服务验证**（等交易时段）

---

## 11. 涉及文件（预估，未开工）

- `dashboard_v2/pricing.py` — 已改完（参考 IV / 对数定价 / basis 标注）
- `dashboard_v2/api_server.py` — 订阅层、tick 采集补 `open_interest`、环形缓冲、监控计算、告警状态机
- `dashboard_v2/risk_engine.py` — 品种级希腊字汇总（规则 5 要用）
- 新增：监控告警模块（是否单独文件、还是并入 `risk_engine.py`）
- 新增：观察期落盘脚本 + 文件
- `Dashboard_设计基线.md` — 定稿后写入告警口径节




设计稿 v0.1迭代版

基于已确立的“F/IV/PnL 与关键结构临界并行为一级触发，Greeks Cash 与操作处方为二级诊断”逻辑，将整套设计方案迭代固化为工程级技术规范。方案拆解为以下六个核心模块：

---

### 一、 数据管道与对齐层（Data Ingestion & Alignment）

该层负责从 CTP 接口和本地文件抽取数据，完成清洗与状态对齐，输出干净的内存对象供计算引擎消费。

```
[CTP 行情 (MD)] ──► 内存 Tick 字典 (F & Options)
[CTP 持仓 (TD)] ──► 内存 Position 列表
[本地结算单 CSV] ──► 历史持仓成本字典 (解决昨仓真实均价缺失)

```

**1. 结算单与 CTP 昨仓对齐逻辑**

* **输入源**：本地 CSV 路径配置于系统启动项，格式为 `[合约代码, 买卖方向, 真实开仓均价, 持仓量]`。
* **对齐规则**：
* **今仓**：直接读取 CTP `Position.open_price`（日内开仓均价）。
* **昨仓**：CTP 默认只提供昨日结算价。系统优先使用本地字典中对应 `合约_买卖` 的真实开仓均价计算 `pnl_history`；若本地未匹配到，降级采用昨日结算价计算，并标记数据源状态为 `fallback`。


* **日内更新**：日内发生平仓时，遵循交易所规则（中金所优先平今，商品按各交易所规则平昨/平今）扣减对应持仓并实时累加日内平仓盈亏。

**2. 期权与期货行情强对齐**

* 标的期货合约与对应期权链采用时间戳窗口对齐（建议 $\Delta t \le 1000\text{ms}$）。
* 若标的期货 Tick 缺失或停牌，期权链 Greeks 计算暂停更新，保持上一有效切片数据并标红提示。
* **盘口清洗**：
* 深度虚值合约若买一价为 0（No-bid），直接取最新成交价作为 $P$；若成交价亦失真，则锁定上一有效 IV。
* 买卖价差超过标的 1% 的宽价差合约，不采用买卖中间价，退化采用最新成交价。



---

### 二、 核心指标计算引擎（Metrics Engine）

全品种统一采用 Black-76 定价模型，标的资产为同月份标的期货。

#### 1. 单合约定价与 IV 求解

* **理论定价与导数（Black-76）**：

$$d_1 = \frac{\ln(F/K) + \frac{1}{2}\sigma^2 T}{\sigma \sqrt{T}}, \quad d_2 = d_1 - \sigma \sqrt{T}$$


$$\Delta_{\text{call}} = e^{-rT} N(d_1), \quad \Delta_{\text{put}} = -e^{-rT} N(-d_1)$$


$$\Gamma = \frac{e^{-rT} \phi(d_1)}{F \sigma \sqrt{T}}, \quad \mathcal{V} = F e^{-rT} \sqrt{T} \phi(d_1), \quad \phi(x) = \frac{1}{\sqrt{2\pi}} e^{-\frac{1}{2}x^2}$$


* **IV 反推保护**：牛顿迭代法上限设为 20 次。若发散或 $\vert{}\mathcal{V}\vert{} < 10^{-4}$，切换为二分法在区间 $\sigma \in [0.001, 3.0]$ 内兜底求解，仍无解则打标 `IV_INVALID`。

#### 2. Greeks 归一化与 Cash 化定义

* **归一化净 Delta（100 乘数当量手）**：

$$L_{\Delta} = \sum_{i \in \text{Options}} \left( \Delta_i \times \frac{\text{Mult}_i}{100} \times \text{Pos}_i \right) + \sum_{j \in \text{Futures}} \left( 1.0 \times \frac{\text{Mult}_j}{100} \times \text{Pos}_j \right)$$



*(IF/IH 权重系数为 3.0，IM 权重系数为 2.0，商品按自身乘数在独立品种内按 1:1 折算)*
* **Delta Cash（金额 Delta）**：

$$\text{Delta Cash} = \sum_{i} \left( \Delta_i \times F_i \times \text{Mult}_i \times \text{Pos}_i \right), \quad \text{Delta 偏离率} = \frac{\text{Delta Cash}}{\text{NAV}}$$


* **Cash Gamma（1% 跳空金额）**：

$$\text{Cash Gamma} = \frac{1}{2} \sum_{i} \Gamma_i \times (0.01 F_i)^2 \times \text{Mult}_i \times \text{Pos}_i$$


* **Gamma 平衡度（两翼凸性比）**：
分别计算 Call 侧与 Put 侧的绝对 Cash Gamma：

$$\text{Gamma 平衡度} = \frac{\max(\vert{}\text{Cash Gamma}_{\text{Call}}\vert{}, \vert{}\text{Cash Gamma}_{\text{Put}}\vert{})}{\min(\vert{}\text{Cash Gamma}_{\text{Call}}\vert{}, \vert{}\text{Cash Gamma}_{\text{Put}}\vert{})}$$


* **Cash Vega（1 Vol 波动率金额）**：

$$\text{Cash Vega} = \sum_{i} \mathcal{V}_i \times 0.01 \times \text{Mult}_i \times \text{Pos}_i, \quad \text{Vega 敞口率} = \frac{\vert{}\text{Cash Vega}\vert{}}{\text{NAV}}$$



---

### 三、 两级预警状态机与处方引擎

采用“三大告警源并行触发 + 二级下钻定性处方”架构。

```
【第一级：三大并行告警源 (Triggers)】
  ├── 驱动源 A (F & IV 异动) ──► 标的 5m 速率超限 / ATM IV 脉冲 / Skew 突破分位
  ├── 结构源 B (动态候选集)   ──► 存在空头单腿 Dist ≤ 1.0 档 / |Δ| ≥ 0.35 / |L_Δ| 偏离
  └── 底线源 C (损益预算)     ──► pnl_today 预算消耗率 ≥ 50% (黄) 或 ≥ 80% (红)
                   │
                   ▼ (任一触发且经防抖滤波确认)
【第二级：结构诊断与量化处方 (Diagnostics & Actions)】
  ├── 宏观受力定性: 校验 Cash Gamma / Cash Vega 评估大盘抗震承载力
  └── 微观指令计算: 输出具体的“期货对冲手数”、“外移行权价”、“残值平仓单”

```

#### 1. 第一级：三大并行触发判定矩阵

| 触发源类型 | 核心指标 | 建议监控区间（不固定） | 业务含义 |
| --- | --- | --- | --- |
| **驱动源 A**<br>

<br>(外部冲击) | 标的位移速率 $v_{\text{approach}}$<br>

<br>平值波动率脉冲 $\Delta \text{IV}_{15\text{m}}$<br>

<br>偏度异动 $\text{Skew}$ | 5 分钟内位移 $\ge 0.5 \sim 0.8$ 个行权档位<br>

<br>短期跳升 $\ge 1.0 \sim 2.0\text{ Vol}$<br>

<br>突破历史 85% ~ 90% 分位数 | 外部行情加速或波动率暴拉，前瞻提示风险，不依赖 PnL 滞后反应。 |
| **结构源 B**<br>

<br>(局部防守) | 动态候选集行权价距离 $\text{Dist}_i$<br>

<br>单腿绝对 Delta $\vert{}\Delta_i\vert{}$<br>

<br>归一化净 Delta 偏离 $\vert{}L_{\Delta}\vert{}$ | $\text{Dist}_i \le 1.0$ 档（交割周收紧至 $\le 1.5$ 档）<br>

<br>$\vert{}\Delta_i\vert{} \ge 0.30 \sim 0.40$<br>

<br>偏离预设绝对手（如 $\pm 8 \sim 15$ 手） | 消除慢速单边行情的漏报盲区，在持仓局部 Gamma 爆发前截击。 |
| **底线源 C**<br>

<br>(绝对止损) | 当日损益预算消耗率<br>

<br>$\text{Burn Rate} = \frac{-\text{pnl\_today}}{\text{Daily Budget}}$ | 消耗达 $40\% \sim 50\%$（黄色预警）<br>

<br>消耗达 $70\% \sim 80\%$（红色熔断） | 账户级终极防线，应对极端跳空与模型失效。 |

* **防抖（Debounce）规则**：驱动源与净 Delta 偏离需满足连续 $N$ 个计算切片（建议 3~5 秒）超限方可报警；单腿平值逼近（$\text{Dist} \le 1.0$ 档）与底线熔断无需防抖，立即触发。

#### 2. 第二级：结构诊断与量化处方

当第一级亮灯后，第二级输出量化诊断并测算执行指令：

* **处方 1：期货锁 Delta（应对单纯方向偏离）**
* 适用：仅 $\vert{}L_{\Delta}\vert{}$ 超限，且无单腿平值逼近。
* 对冲手数：

$$\text{Hedge Lots} = -\text{round}\left( \frac{L_{\Delta}}{\text{Mult}_{\text{Fut}} / 100} \right)$$




* **处方 2：平仓外移 Roll Out（应对局部单翼受压）**
* 适用：动态候选集命中受压腿（$\text{Dist} \le 1.0$ 档）。
* 动作拆解：全量对价平仓当前合约 $K_{\text{current}}$；顺势开仓外移至 $K_{\text{new}} = K_{\text{current}} \pm 2 \times \text{StrikeStep}$。


* **处方 3：残值止盈 Close Penny（应对虚值尾矿堆积）**
* 适用：深度虚值空头单价 $\le 3 \sim 5$ 个最小变动价位。
* 动作拆解：以买一价挂单全平，释放保证金与张数限额。


* **处方 4：硬熔断 Hard Stop（应对损益击穿）**
* 适用：$\text{Burn Rate} \ge 80\%$。
* 动作拆解：撤销所有挂单，利用期货反向锁死总 Delta 或按流动性规则顺序减仓。



---

### 四、 保证金联动与动态矩阵配置

根据实时保证金占用率（$\text{Margin Ratio} = \text{占用保证金} / \text{NAV}$），动态调整风控参数的收紧程度：

```
[预设基准仓位: 25% ~ 30%] ──► 标准运行区 (标准阈值)
[超额占用: 30% ~ 35%]     ──► 压缩收紧区 (阈值收紧 30%)
[高危临界: > 35%]         ──► 强制减仓区 (阈值收紧 50% + 阻断新开仓)

```

**动态参数映射矩阵（建议参考值）**：

| 保证金占用率区间 | Delta 偏离率容忍度 | Gamma 平衡度上限 | Gamma 控制敞口上限 | 候选集移仓警戒线 |
| --- | --- | --- | --- | --- |
| **$< 25\%$ (充裕)** | $\pm 2.5\%$ | $\le 1.6$ | $\le 10\%$ | $\le 1.0$ 档 |
| **$25\% \sim 30\%$ (基准)** | $\pm 2.0\%$ | $\le 1.5$ | $\le 10\%$ | $\le 1.0$ 档 |
| **$30\% \sim 35\%$ (警戒)** | $\pm 1.2\%$ | $\le 1.3$ | $\le 8\%$ | $\le 1.5$ 档 |
| **$> 35\%$ (超限)** | $\pm 0.8\%$ | $\le 1.2$ | $\le 6\%$ | $\le 2.0$ 档 |

---

### 五、 树形聚合与前端交互数据契约

后端完成全部聚合与打标，前端仅负责纯渲染与交互折叠。

#### 1. 三层树形分组架构

* **L1 品种层（Product Level）**：如 `IF`、`MO`、`CU`。聚合该大类下所有月份期货与期权的总金额 Greeks、归一化 Delta、Gamma 平衡度及 `pnl_today`。
* **L2 到期月份层（Expiry Level）**：如 `IF2609`。聚合该月份下期货及期权各腿的 Greeks 与风险暴露。
* **L3 合约明细层（Contract Level）**：单条合约明细，标注行权价、距离档位、单腿 Delta、单价及残值标记。

#### 2. 后端状态打标规范（CSS Class Injection）

后端根据阈值直接注入 `_tag` 字段，前端直接绑定样式名：

* `normal`：绿色/无高亮（运行于安全区间）。
* `warning`：黄色高亮（接近临界，如 $\text{Burn Rate} \ge 50\%$ 或 $\text{Dist} \le 1.5$ 档）。
* `danger`：红色闪烁（已突破硬风控，如 $\text{Dist} \le 1.0$ 档或净 Delta 超限）。

#### 3. 统一 API 输出格式（`/api/dashboard`）

```json
{
  "summary": {
    "nav": 3334380,
    "margin_ratio": 0.2709,
    "pnl_today": 1287,
    "pnl_history": 409377,
    "pnl_burn_rate": 0.05,
    "pnl_status": "normal",
    "last_update": "14:28:10"
  },
  "market_drivers": {
    "underlying_symbol": "IF2609",
    "underlying_price": 4125.4,
    "underlying_5m_move": 0.12,
    "atm_iv": 17.2,
    "iv_15m_shock": 0.3,
    "skew": 2.1,
    "skew_status": "normal"
  },
  "tree": [
    {
      "key": "IF",
      "name": "沪深300大类",
      "type": "L1_PRODUCT",
      "metrics": {
        "norm_delta_lots": 2.1,
        "norm_delta_tag": "normal",
        "delta_cash": 69664,
        "delta_cash_pct": -0.0209,
        "gamma_balance": 1.18,
        "gamma_balance_tag": "normal",
        "gamma_exposure_pct": 0.0186,
        "gamma_exposure_tag": "normal",
        "vega_cash": -15200,
        "pnl_today": 1287
      },
      "active_candidates": [
        {
          "symbol": "IO2609-C-4700",
          "strike": 4700,
          "distance_step": 1.1,
          "delta": 0.32,
          "status": "warning"
        }
      ],
      "actions": [
        {
          "level": "INFO",
          "action_type": "MONITOR",
          "description": "当前持仓结构健康，IO2609-C-4700 逼近 1 档区间，注意观察"
        }
      ],
      "children": [
        {
          "key": "IF2609",
          "name": "2609月份",
          "type": "L2_EXPIRY",
          "metrics": {
            "norm_delta_lots": 2.1,
            "gamma_balance": 1.18
          },
          "children": [
            {
              "key": "IO2609-C-4700_short",
              "symbol": "IO2609-C-4700",
              "type": "L3_CONTRACT",
              "direction": "空",
              "volume": 22,
              "last_price": 14.2,
              "distance_step": 1.1,
              "delta": -0.32,
              "gamma": -0.0008,
              "vega": -180.5,
              "is_penny": false,
              "row_tag": "warning"
            }
          ]
        }
      ]
    }
  ]
}

```

---

### 六、 实盘工程关键漏洞与防范方案

**1. 昨仓开仓成本缺失与换日清空**

* **漏洞**：若交易员未上传最新结算单，昨仓盈亏无法计算。
* **防范**：系统启动时若检测不到当日匹配的结算单文件，强制使用 CTP 昨日结算价填充，并在全局看板常驻黄色警告：“结算单未对齐，历史盈亏为估算值”。交易日换日（15:00 后及次日 08:30 前）自动触发历史快照归档与内存重置。

**2. 深度虚值无买盘时的 IV 穿透与发散**

* **漏洞**：极虚值合约买盘为 0，市价单边剧烈跳动导致 IV 在 0% 与 200% 间振荡，引发 Cash Vega 和 Gamma 的剧烈虚假警报。
* **防范**：引入单合约状态机：当 `bid_price == 0` 时，锁定该合约的 IV 为前值，禁止重新反推 IV；仅更新标的 $F$ 变化带来的固有 Greeks 漂移。

**3. 高并发切片下的数据一致性锁机制**

* **漏洞**：CTP 行情推送线程（MD）与 Flask API 响应线程并发访问共享内存字典，会导致前端读取到半更新状态的树（如父节点已汇总，但子节点尚未计算完毕）。
* **防范**：采用**双缓冲原子替换（Double-buffering / Atomic Swap）**：
* 后台计算线程在独立的 Shadow Copy 中完成清洗、定价、Greeks 测算与树形拼装；
* 全部指标完成后，执行一次原子指针替换：`global_snapshot = new_snapshot`；
* Flask 接口读取快照时不加全局粗粒度互斥锁，彻底杜绝 UI 阻塞与数据不一致。

> **核实结论（双缓冲）**：现状**已是**影子构造 + 发布。`api_server.py:1051` `build_tree` 为纯函数、每轮新建整树；`:1087-1095` 七个字段在同一把 `_shared_lock`(RLock) 内写完；`_snapshot()`（`:259`）同锁读。不存在"API 现场遍历正在变化的 dict"。
> **需立的规矩（新增一条硬约束）**：`_snapshot()` 返回的是**同一 tree 对象引用**（非深拷贝），当前安全仅因下一轮构造新对象。故规定：**tree 发布即只读（write-once）**，一切就地补丁必须在 `:1087` 发布之前完成；`:1084` 那类 `tree["summary"][...] = ...` 严禁出现在发布之后。不引入新并发机制。

---
---

# 设计稿 v0.2（定稿 · 权威版本）

日期：2026-09-20。本节为准，**与上文（v0.1 及"迭代版"）冲突处，一律以本节为是**。上文段落保留作讨论痕迹，被本节作废的段落见 §六。

三方审计采纳情况：8 条中 7 条照收，第 2 条（Greeks 全进二级）打折执行——净 Delta 留在一级。审计未抓到的 a/b/c/d/f/g 六处硬伤见 §六。

---

## 一、一级触发四源（定稿，勿再漂）

```
① F  5m 速率      标的位移速率（对数收益，IV·期限归一）
② ATM IV 5m 速率  品种级参考 IV 的脉冲
③ 结构风险        单腿 |换算Δ| ≥ Y  或  品种净 Δ 超限（后者【预留】）
④ Premium Burn    -pnl_today / 昨日净收取权利金
```

- Gamma / Vega / Skew / Margin **不独立触发**
- Margin（账户级）**只改变头寸侧容忍度**（③④的阈值），**永不改变** ①② 的行情异常阈值——市场是否异常与仓位多大无关
- 四源并行、各自独立状态机，任一越线即报

### 1.1 代码词汇表（2026-09-21 定名，勿再漂）

| 概念 | 配置键（`alert_settings.json`） | 告警源键（`source`） | 前端显示 |
|---|---|---|---|
| F 5m 速率 | `f_rate_warn` / `f_rate_danger` | `f_rate` | F速率 |
| ATM IV 速率 | `iv_rate_warn` | `iv_rate` | IV速率 |
| 结构风险（单腿换算Δ） | `conv_delta_warn`（红档=实值，无阈值键） | `conv_delta` | 换算Δ |
| Premium Burn | `burn_warn` / `burn_danger` | `burn` | Burn |
| 风险度 | `margin_ratio_warn` / `margin_ratio_danger` | `margin` | 风险度 |
| 品种净 Δ | `net_delta_warn`【预留·禁用=10000】 | —（本期无） | —（前端不显示） |

**两个 Δ 不是一个东西（维护坑点）**：
- **市场 Δ** = 看板 Δ 列、L3 节点 `delta` 字段（`pricing.py` 写 `pos_delta`）→ **不参与任何告警判定**
- **换算Δ** = 固定 σ_ref 的 Black-76 Δ，后端 `_conv_delta()` 现算 → 判定专用（平值腿选择 + 结构告警）
- 源键刻意**不叫** `delta`，配置键刻意**不叫** `delta_leg_warn`，就是为了杜绝两者混用

## 二、二级诊断（只解释，不抢触发）

告警亮灯后下钻展示：Delta Cash、Gamma PnL @1%、**Gamma Imbalance**、Vega PnL @+1 vol、Skew、Margin Ratio。

**Gamma Imbalance（改定，弃迭代版 max/min）**：

```
GammaImb = (|Γ_put| − |Γ_call|) / (|Γ_put| + |Γ_call|)      值域 [−1, 1]
  0    两翼平衡
 +1    完全 Put 侧
 −1    完全 Call 侧
 两侧绝对额均 < ε → 显示 N/A（不显示数值，不参与诊断）
```

保留"哪一翼失衡"的方向信息；分母为和，不会趋零爆无穷。原 `max/min` 作废。

## 三、阈值单位：弃"档"，改用 Δ / σ√T 归一（裁决）

**理由**：档距会因行权价段变化而跳变（同一标的不同价格段档距不同），跨品种也不可比；股指期权档更小，问题更突出。

- **单腿逼近（触发③）**：用 `|换算Δ| ≥ Y`，Y 暂定 **0.42**（观察期标定）；配置键 `conv_delta_warn`，告警源键 `conv_delta`（见 §一 1.1）。红档不设阈值——该腿越界实值（K 与 F 异侧）即红。
  - **换算Δ 定义（2026-09-21 定案）**：**固定 σ_ref 作为 IV**、按该合约**自身剩余到期**、用 Black-76 算出的 Δ。市场 Δ 会随 IV 漂移（同一条腿 IV 抬升就把 Δ 推离 0.5），换算Δ 只随 F/K/T 变，口径稳定，专门用来衡量「行权价离标的有多远」。
  - **看板 Δ 列仍显示市场 Δ**（列名就叫「Δ」）；告警里报的 Δ 一律叫「换算Δ」，两者不是一个东西。
  - 两个消费者：① 平值腿选择（`_atm_iv_of_group` 选腿）② 结构告警单腿 |换算Δ|。共用同一个 σ_ref。
  - 缺 F/K/到期 → 该腿不参与判定（不退回市场 Δ，避免口径混用）；仅平值腿选择允许退回市场 Δ 兜底。
  - 不再需要 v0.1 的 `0.10×σ×√(20/365)` 常数判据（**退役**）。"距平值 X 档"降级为**展示字段**，不作阈值。
- **品种净 Δ（触发③）**：【预留】本期不实现品种层净 Δ 监控（多腿结构如背对/领口下口径未定，详见讨论）。未来再议，届时确定分子分母口径（期权腿 vs 全持仓）、锚点基准（昨日收盘）与阈值策略。当前后端 `net_delta_warn=10000`（实质禁用），前端不显示该输入框。
- **F 速率（触发①）**：`ln(5min窗口 H/L) / (σ_ref × √(5min/年))`，**单位 = 倍**，分母 √ 内 = `5 / (250交易日 × 60min × 日交易总时长h)`，日交易总时长查 `config/trading_hours.csv`（查不到降级 4h 并提醒）。含义：窗口极差 = 多少个「5min 正常波动单位」。旧的 `|ΔlnF|/(σ√T)`（随剩余到期变）**已退役**。阈值起点 黄 3.0 / 红 5.0（布朗运动 5min 极差期望 ≈1.6σ，故 ~1.6 是常态），观察后标定
- **IV 速率（触发②）**：`(5min窗口 IV 极差) / σ_ref`，**单位 = %**（前端阈值框里直接填百分号上的数字，预设 5）。基数就是**该品种的 σ_ref**（与 F 速率、换算Δ 共用同一个数），不另做昨收快照合成。起点 5% = 5min 内 IV 摆动达 σ_ref 的 5%（AU σ_ref=30 vol 点 → 1.5 vol 点），观察期标定
- **Premium Burn（触发④）**：分母 = **昨日收盘持仓净收取权利金**（不是 Daily Budget，该词作废）。阶梯 **0.20 黄 / 0.50 红**。仅 `net_premium > 0`（净卖方）启用，净买方不监控

## 四、状态机（沿用 v0.1 #7，纠正迭代版的"连续 N 次才触发"）

```
静音 ──[单点越线]──> 告警 ──[连续 N 采样回落]──> 静音
                        └──[持续越线]──> 保持（不重发，只累计"已持续 X 分钟"）
```

- **触发零延迟**：任一采样越线立即报，宁滥勿缺。迭代版"驱动源需连续 N 切片确认"作废
- 该条成立的前提：①② 都是 **5 分钟窗口统计量**，非单 tick 差分，窗口本身已稀释毛刺。若将来改用瞬时速率，此前提失效，需重议防抖
- **解除**：连续 N 采样回落，N 起点 3
- **推送**：仅状态跳变时发（静音→告警、告警→静音）
- **本版全部仅提示、不操作**：不阻断开仓、不自动下单、不自动撤单。处方只输出建议文本与手数

## 五、观察期指标口径（数据侧定义）

- **品种级 σ**：沿用 `_pick_ref_iv` 输出（同 (标的,到期) 组内 put `K<F` 最大 + call `K>F` 最小 两腿中位数）= ATM IV，**非 VIX**。VIX 式方差互换积分需订 ITM 两翼，与"ITM 不订"冲突，**降级为远期备选**
- **σ basis 分级 + 冻结规则**：σ 来源若落到**兜底级**（主力月回退 / 20% 默认），则该采样点 `d(lnσ)` **不可信 → 冻结触发②**（保持上一状态，不新报也不解除）。平值成交级 / 模型价夹盘口级正常参与。这是"无成交=无价"原则在告警侧的延伸
- **标的 5m 位移** = `ln(F_t / F_{t−5min})`，F 取**标的前月期货最新价**（非现货/指数）
- **长窗幅度**：基准 = **昨收**（v0.1 #5 不变）；但长窗**只做展示与诊断**，不作一级触发（一级要的是反应速度）
- **采样频率**：标的每 tick（约 500ms~1s）；参考腿 **3~5 秒降频** → σ 与 IV 速率均为 3~5 秒一个点。**两者速率窗口各自对齐到 5 分钟，但计算频率不同步**：触发①③④每 tick 可算，触发②按 3~5 秒节奏算

## 六、作废段落清单（迭代版）

| 项 | 迭代版位置 | 处置 |
|---|---|---|
| **a** | §六.1「15:00 后及次日 08:30 前换日归档」 | **作废**。股票期权思路；商品夜盘 21:00 起已属**下一交易日**，按此时点会误归档、错清 `pnl_today` 基准。改：一律按 CTP `TradingDay`，不按本机时钟（与现有 `bd` 缺口同源，同批修） |
| **b** | §1.2 盘口清洗 + §六.2「bid=0 锁定上一有效 IV」 | **作废**。与已落码 `pricing.py` 重复：`basis` 分级、`_is_stale` 整条作废、`_model_in_bracket` 参考 IV 模型价夹盘口 已解决振荡问题。"锁 IV"是新增状态机，代价是**真波动被冻住 → 漏报**，比振荡更难查。替代方案见 §五 的 σ basis 冻结（冻结**告警**，不冻结**估值**） |
| **c** | §1.1「本地结算单 CSV 供真实开仓均价」 | **数据源改回三件**：CTP 持仓 + 成交账本（基线 §3.8）+ 收盘快照/结算单三级降级（§3.7，快照为基准）。新开第四个源会与本已定的降级链口径打架 |
| **d** | v0.1 §8「数据前置缺口」整节消失 | **恢复**。订阅层（OTM侧+平值、不订 ITM、OI 选月、参考腿降频）、环形缓冲、`open_interest` 未采集、现有快照不含时间戳/bid/ask —— 不补完，①②根本算不出来。JSON 契约漂亮但底下没有时间序列 |
| **f** | §四 Gamma 平衡度 `max/min` | **作废**，改 §二 的有向 Imbalance |
| **g** | §2.1 牛顿迭代 20 次上限 | **作废**。已是严格二分 + 可及区间 [0.1%, 500%]，无解出 NaN。牛顿在深虚值/近零 Vega 区发散，后面仍要接二分 = 绕回现状，多写一套 |
| — | §六.3 双缓冲 | **降级为一条纪律**（见本节顶部核实结论），不是新工项 |
| **e** | IF/IH 3.0、IM 2.0 权重 | **保留**。用户确认做股指期权，100 当量手归一化必要 |

## 七、落地顺序（微调，采纳第三方第 2 点）

```
0  修 settlement 两个 bug（_local_complete 一票否决 / sync 只在连接时触发）
   + 换日改按 TradingDay  + 重启验证 pricing.py
   ← pnl_today 基准不干净，触发④是空的
1  数据层：订阅集合(OTM侧+平值 / OI 选月 / 参考腿 3-5s 降频)
   + open_interest 采集 + _poll_once 改批量读 + 环形缓冲(60-100 点)
2  σ + basis 分级 → 观察期：只落盘不报警，跑分位数标定
3  ★ 风险指标计算层：leg Δ / 品种净 Δ（归一当量手）/ Dist 展示量 / GammaImb
   （不必独立模块，但状态机必须先有它 —— ③ 依赖稳定 Δ）
4  一级四源 + 状态机（触发零延迟 / N 采样解除 / 跳变才推）
5  二级诊断：Delta Cash / Gamma PnL / Vega PnL / Skew / Margin
6  处方（仅建议）：对冲手数直算；Roll 改「找最近 |Δ_new| ≤ 目标的流动腿」
   （弃固定"外移 2 档"）；残值平仓提示
7  三层树 L1 品种 / L2 到期月 / L3 合约 + 后端 `_tag` 注入
```

依赖关系：`0 → 1 → 2`，`3` 依赖 `1`（要 Δ 就要全链行情），`4` 依赖 `2+3`，`5/6/7` 依赖 `4`。
**当前卡点在 1**：没有时间序列，速率类指标一律算不出。

## 八、数据契约补充（迭代版 §五 沿用，改这几处）

- `pnl_burn_rate` 分母字段名改 `net_premium_yesterday`（不用 `daily_budget`）
- `gamma_balance` → **`gamma_imbalance`**（[−1,1] + `gamma_imbalance_tag`；两侧近零时值为 `null` + `tag="na"`）
- `market_drivers.underlying_5m_move` 语义改为**归一化位移**（`ΔlnF/(σ√T)`），另加 `underlying_5m_move_pct` 存 % 读数
- 新增 `iv_basis`（`atm_trade` / `model_bracket` / `front_month` / `default_20`），值落到后两级时**触发②冻结**
- 新增 `alerts[]`：`{source: f_rate|iv_rate|conv_delta|burn|margin, underlying, symbol, level, value, threshold, v_from, v_to, msg, ts}`（`source`/配置键定名见 §一 1.1；实现字段以此为准）
- L3 合约节点保留 `distance_step` 作展示，**删除**它作阈值的任何引用；判据改用**换算Δ**——由后端 `_conv_delta()` 现场计算，**不写回 L3 `delta` 字段**（该字段仍是市场 Δ，供 Δ 列显示）

---

## 八、品种级 σ_ref 配置（最终版，剔除所有采集任务）

```markdown
- **存储**：`config/iv_sigma_ref.csv`（单值不分月，人工填写一次永久有效直到修改）
  ```csv
  symbol,sigma_ref
  IF,0.185
  AU,0.245
  SC,0.310
  ```
- **读取与降级规则**：
  1. **优先级**：配置表 → 默认 25%
     - 若某品种在表中缺失 → 自动用 **默认值 0.25**
     - 若 `sigma_ref` 为空字符串 → 视为缺失，同样用 0.25
  2. **弹窗提醒机制（纯前端，不推送任何外部渠道）**：
     - **时点**：首次加载时，或每月最后一个交易日 **15:05 盘后**刷新页面时
     - **触发条件**：品种表中有品种缺 `sigma_ref`（空/无行），且**该品种当月有持仓**
     - **行为**：前端弹一次对话框，列出待填品种 + 当前生效值（默认 0.25），可直接在框内填并保存 CSV
     - **豁免**：全月无持仓的品种不提示（空仓不需重估 Δ）
     - **禁止**：不发 Telegram / 不发声音 / 不做轮询骚扰。缺失时后端一律静默用 0.25 兜底，看板顶部挂一行灰字状态即可
  3. **执行期限**：本规则作为**临时措施**，从今日起执行，直到系统具备自动采集历史 IV 序列并计算 80% 分位的能力为止（届时转为全自动，不再依赖人工填写）。过渡期内，σ_ref 的准确性依赖手动更新。

---

## 七 bis、观测与阈值标定路径（剔除数据采集后版本）

v0.1 §七 和 v0.2 §八 原本的设计是：观察期只落盘→跑分位数→定阈值。但现在**剔除所有数据采集落盘任务**，这条路径失效。

**替代方案**：
- **经验值起步**：
  - F 速率归一阈值：候选 **0.3**（1 个波动单位/5min）或 **0.5**（半日内一个完整波动）
  - IV 速率阈值：**1.5 vol/5min**
  - 净 Δ 限额：±**10 手**（按品种名义值可微调）
  - Burn：0.20 黄 / 0.50 红（不变）
- **事后人工校准**：告警开启后，根据实际运行情况（误报/漏报频率）手动调整这些数字，不需要落盘统计支持
- **日志复用**：现有 `api_server_run.log` 一直有运行信息，可以手工 grep 出关键事件复盘，不新建 jsonl/parquet 文件

**结论**：剔除采集后，v0.2 直接进入**经验值试运行阶段**，放弃分位数标定路径。后续如需自动化，再单独立项补全。

## 十、与 v0.1 已定案的差异（一览）

| v0.1 条目 | v0.2 |
|---|---|
| #8 ITM 判据 `0.10×σ×√(20/365)` | **退役**，改 `|换算Δ| ≥ Y`（换算Δ = 固定 σ_ref 的 Black-76 Δ） |
| #7 捕捉即报、持续性只管解除 | **保留**（迭代版改的"连续 N 次才触发"作废） |
| #2/#3/#4 两张 2×2（速率图/幅度图，X=标的 Y=IV，共振/背离/无关） | **保留为观察图**，不进一级触发；长窗幅度降级为展示 |
| #5 长窗基准=昨收 | 保留，但只服务展示/诊断 |
| #6 短窗 5 分钟 | 保留 |
| #9 净买方不管 | 保留 |
| #10 盈亏 20% | 细化为 **0.20 黄 / 0.50 红** |
| Q1 IV 基准甲/乙 | **甲**（品种级 ATM IV = `_pick_ref_iv`），乙降远期 |
| Q3 合约粒度 | 只有**单腿绝对 Delta** 到合约级，其余全品种级；账户级只看 Margin |
| Q4 "控制" | **本版仅提示，不操作** |
| Q6 单位统一 | 统一对数；F 再除 `σ√T` 归一 |
| Q7 档的定义 | **不再需要**（弃档作阈值） |
| Q13 分品种阈值 | 靠归一化解决大半，剩余仍分品种标定 |