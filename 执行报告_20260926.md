# 执行报告（2026-09-26，按审查意见执行，不动手前已逐项确认）

执行顺序与状态（按用户确认的 P1 → 暂不动顺序）：

已完成（4 项，逐项回报）：
1. iv_basis / iv_source（P1 完成）
   - pricing.py L372-377：计算 iv 时确定 iv_source（market / ref_iv / default）
   - pricing.py L403：results 增加 'iv_source': iv_source
   - api_server.py L1847：_atm_iv_of_group() 过滤 iv_source == 'default'（不入告警计算）
   - 结果：monitor 层只计算 market/ref_iv 的 IV 速率，default 不触发告警

2. 告警解除 N=3（P1 完成）
   - api_server.py L2108-2128：解除逻辑增加 normal_counts 连续计数
   - 触发端（_push）保持无 debounce（单次越线立即触发）
   - 解除端：连续 3 采样正常（prev_active 有值但 cur_active 无值，计数 >=3）才记录 normal 并弹窗
   - 仍触发时重置计数为 0

3. settlement _valid_dates() 修正（P1 完成）
   - settlement.py L275 _missing_dates()：scanned 改用 _valid_dates()（非 _scanned_dates()）
   - settlement.py L327 sync() 当天下载判断：today_str not in _valid_dates()（非 _scanned_dates()）
   - 结果：缺失判断基于有效 schema，不再因文件名存在但内容无效而漏补

4. 文档同步（P1 完成，4→5→6 三点同步到基线）
   - 数据流 §一：Atomic Swap → RLock 保护 shared_state（_shared_state 写，API 读）（已修改）
   - 数据流 §一 计算步骤 2：三级 → 四级阶梯 + 兜底（已修改）
   - §3.2 IV 链路描述：保留四级规则（已存在），implied_vol_bisection() 描述更新（增加 strict 参数说明，明确 strict=True 为当前主路径，default 不静默造 20%）（已修改）
   - 结果：文档与代码一致，无重构双缓冲（按用户要求，不重构）

暂不动（已冻结，不修改代码或文档）：
- 净 Delta 告警恢复（net_delta_warn=10000 禁用状态，用户确认不恢复）
- Dist 加入结构触发（用户确认只做展示指标，不参与触发）
- G3 calc_adjust_price() 命名（用户确认暂不动，功能已实现）
- F/IV 组合监控（用户确认暂不做，现有只有单项 F rate / IV rate）
- Burn 重构（用户确认暂不动，后续单独重做）
- 双缓冲重构（用户确认暂不动，锁模式暂够用）
- 设计基线中未同步的内容（重构设计 v4 的 pnl_daily 残留、文件路径差异）——用户要求只同步实际实现到基线，不重写重构文档，因此重构文档差异未处理（按用户“暂不动”范围处理）

检查状态：
- 代码修改：pricing.py（2 处）、api_server.py（2 处）、settlement.py（2 处）、Dashboard_设计基线.md（4 处同步）
- 无新增临时文件/脚本（按规则：未准不新建）
- 所有修改前已逐项确认方案，无擅自动手
- 工具报错：无（patch 均成功）
